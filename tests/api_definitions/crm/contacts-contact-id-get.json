{
  "name": "GET /api/crm/contacts/:contact_id — sdk-crm",
  "endpoint": "/api/crm/contacts/:contact_id",
  "method": "GET",
  "description": "Fetches a single CRM contact by contact_id. Edge cases: an unknown contact_id returns 404; the route takes no tenant_id param and does not re-check tenant ownership beyond the JWT gate, so QA should confirm cross-tenant reads are blocked upstream; a malformed (non-UUID) contact_id fails the Postgres uuid cast and surfaces as an unhandled 500, not a 404.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in the form \"Bearer <jwt>\""
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "JWT signature invalid, malformed, or past its exp claim"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "NotFound",
      "when": "no crm contact row exists for the given contact_id"
    }
  ],
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/auth/register",
    "POST /api/app-identities",
    "POST /api/memberships",
    "POST /api/personas",
    "POST /api/crm/contacts"
  ],
  "expectedStatus": 200,
  "sdk": "sdk-crm",
  "sourceFile": "packages/sdk-crm/src/server/routes.ts",
  "_generated": true,
  "testability": "auto",
  "testCases": [
    {
      "name": "Fetch an existing contact by id",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "contact_id": "{{cache:crm.contacts.response.data.contact.contact_id}}"
      },
      "expectedStatus": 200
    }
  ]
}
