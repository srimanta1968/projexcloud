{
  "name": "GET /api/connectors — api-gateway",
  "endpoint": "/api/connectors",
  "sdk": "sdk-connectors",
  "method": "GET",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sourceFile": "services/api-gateway/src/app.ts",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "testability": "auto",
  "testCases": [
    {
      "name": "List connector installs for a tenant (tenant-scoped query)",
      "queryParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "expectedStatus": 200
    }
  ]
}
