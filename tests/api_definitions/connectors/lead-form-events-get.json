{
  "name": "GET /api/connectors/lead-forms/:tenant_id — archived lead-form deliveries, including rejections",
  "endpoint": "/api/connectors/lead-forms/:tenant_id",
  "sdk": "sdk-connectors",
  "sourceFile": "packages/sdk-connectors/src/server/routes.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "552a31f9-7fc7-4353-9f96-41823a26464a",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "featureId": "0204ef48-4a2a-4316-a58b-8f1a05cb16ca",
  "epicId": "84ab818d-aeb2-47b4-ba3c-fb4b0b5662c4",
  "priority": 91,
  "testability": "auto",
  "dependsOn": ["POST /api/auth/signup-tenant"],
  "coversCriterion": ["552a31f9-7fc7-4353-9f96-41823a26464a#AC4"],
  "description": "Lists archived lead-form deliveries for a tenant, filterable by platform and outcome (accepted | rejected | duplicate). Proves AC4 — the triage view exists, and every row reports has_raw, so an operator can confirm the payload is still recoverable before attempting a re-process. Unlike the ingest endpoint this IS tenant-authed: the ingest caller is a platform proving itself with an HMAC, whereas reading a tenant's archived lead payloads is ordinary tenant data access and the default-deny gate applies (MUST-52). QA edge cases: rejected deliveries are the primary reason to call this, so outcome=rejected is the interesting filter and its rows always carry has_raw true — a flag with no reader would make 'archived on rejection' meaningless; the raw payload itself is deliberately NOT returned in the list, since these carry customer PII and a list endpoint should not spray it, so the caller fetches or re-processes a specific event instead; limit is clamped to 1..500; results are newest-first; an unknown platform filter simply matches nothing rather than erroring, since the filter is a narrowing rather than an assertion.",
  "testCases": [
    {
      "name": "List all archived deliveries for the tenant",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "pathParams": { "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}" },
      "expectedStatus": 200
    },
    {
      "name": "Triage view — only the rejected ones",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "pathParams": { "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}" },
      "queryParams": { "outcome": "rejected" },
      "expectedStatus": 200
    },
    {
      "name": "Filter by platform",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "pathParams": { "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}" },
      "queryParams": { "platform": "meta", "limit": "50" },
      "expectedStatus": 200
    }
  ],
  "errorCases": [
    {
      "name": "Unauthenticated is refused by the default-deny gate",
      "status": 401,
      "code": "Unauthorized",
      "message": "missing or invalid tenant token",
      "when": "no valid tenant Bearer token — reading archived lead payloads is tenant data access, unlike the HMAC-gated ingest endpoint",
      "pathParams": { "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}" },
      "expectedStatus": 401
    }
  ]
}
