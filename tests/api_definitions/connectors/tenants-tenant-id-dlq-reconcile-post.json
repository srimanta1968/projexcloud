{
  "name": "POST /api/connectors/tenants/:tenant_id/dlq/reconcile - Reconcile DLQ state",
  "endpoint": "/api/connectors/tenants/:tenant_id/dlq/reconcile",
  "sdk": "sdk-connectors",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "sourceFile": "packages/sdk-connectors/src/server/routes.ts",
  "taskId": "e17da8c8-e873-4414-b68b-6eeaa47accee",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "priority": 33,
  "testability": "auto",
  "description": "Reconcile duplicate/partial connector-sync DLQ state for a tenant. Collapses superseded duplicates (several active dlq/retrying entries sharing the same install_id + sync_kind + external_ref — only the newest is kept, older ones marked resolved) and requeues entries stuck in 'retrying' past the stale window (worker crashed mid-drive) back to 'dlq'. Idempotent: returns {superseded:0, requeued:0} on a clean queue.",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "testCases": [
    {
      "name": "Reconcile a tenant's DLQ (clean queue -> zero counts)",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "payload": {},
      "expectedStatus": 200,
      "expectedResponse": {
        "data": { "superseded": "number", "requeued": "number" }
      }
    }
  ],
  "errorCases": [
    { "status": 401, "code": "Unauthorized", "message": "missing or invalid token", "when": "no valid Bearer token is supplied" }
  ]
}
