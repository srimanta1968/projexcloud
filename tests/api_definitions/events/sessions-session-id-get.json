{
  "name": "GET /api/events/sessions/:session_id — sdk-event",
  "endpoint": "/api/events/sessions/:session_id",
  "method": "GET",
  "description": "Fetches one event session with its capacity and current sold_count - the pair QA needs to verify oversell protection. Edge cases: an unknown session_id returns 404; the route applies no tenant filter of its own beyond the JWT gate, so cross-tenant readability should be verified; a malformed (non-UUID) session_id fails the uuid cast and surfaces as an unhandled 500 rather than a 404.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in the form \"Bearer <jwt>\""
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "JWT signature invalid, malformed, or past its exp claim"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "NotFound",
      "when": "no event.session row exists for the given session_id"
    }
  ],
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "POST /api/vault/keys",
    "POST /api/encounters",
    "POST /api/events/sessions"
  ],
  "expectedStatus": 200,
  "sdk": "sdk-event",
  "sourceFile": "packages/sdk-event/src/server/routes.ts",
  "_generated": true,
  "testability": "auto",
  "testCases": [
    {
      "name": "Fetch a session by id",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "session_id": "{{cache:events.sessions.create.response.data.session.session_id}}"
      },
      "expectedStatus": 200
    }
  ]
}
