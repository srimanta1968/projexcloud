{
  "name": "GET /api/analytics/datasets/:spec_id/builds — dataset reproducibility ledger",
  "endpoint": "/api/analytics/datasets/:spec_id/builds",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "e292d33e-d950-446d-9a78-22818cc31e4c",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "priority": 54,
  "dependsOn": ["POST /api/auth/signup-tenant", "POST /api/analytics/datasets"],
  "testability": "manual",
  "skipReason": "Needs a real sub-bearing JWT and a registered dataset spec. Returns each build's window + lineage_ref (sdk-lineage edge) so a training dataset can be reproduced and traced back to its source asset.",
  "testCases": [
    {
      "name": "List builds with lineage refs",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "pathParams": { "spec_id": "{{cache:analytics.dataset.response.data.spec_id}}" },
      "expectedStatus": 200,
      "testability": "manual"
    }
  ]
}
