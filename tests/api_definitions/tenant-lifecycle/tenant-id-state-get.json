{
  "name": "GET /api/tenant-lifecycle/:tenant_id/state - Read tenant lifecycle state",
  "endpoint": "/api/tenant-lifecycle/:tenant_id/state",
  "sdk": "sdk-tenant-lifecycle",
  "sourceFile": "packages/sdk-tenant-lifecycle/src/server/routes.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "10360bb1-eaa7-4d76-999b-57d60662885b",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "a5489f77-208c-47d4-9662-93a2a183a4c1",
  "epicId": "d83f7b6e-821c-4fe1-96e6-910eca8fbbfc",
  "priority": 45,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/tenant-lifecycle/:tenant_id/suspend"
  ],
  "testCases": [
    {
      "name": "Read state after a transition",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "expectedStatus": 200
    }
  ],
  "fieldEnums": {
    "current_state": [
      "active",
      "suspended",
      "offboarding",
      "offboarded",
      "sandbox"
    ]
  },
  "description": "Reads the current lifecycle state record for a tenant (active / suspended / offboarding and its associated metadata such as reason, actor and deadline). Edge cases: ownership is enforced before the read — the JWT tenant_id or parent_tenant_id must equal the path tenant_id, so probing another tenant's state returns 403, not 404; a tenant the caller does own but which has no lifecycle row yet (never suspended, reinstated or offboarded) returns 404 NotFound rather than a synthesized \"active\" default, so consumers must treat 404 as \"no lifecycle event recorded\"; a malformed tenant_id fails the ownership comparison and returns 403 rather than a validation error.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "No Authorization: Bearer <jwt> header on a requireAuth route"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past)"
    },
    {
      "status": 403,
      "code": "Forbidden",
      "message": "Tenant ownership check failed",
      "when": "The JWT's tenant_id and parent_tenant_id both differ from the :tenant_id in the path"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "NotFound",
      "when": "getState returns no lifecycle row for the supplied tenant_id"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "Internal Server Error",
      "when": "The database is unreachable or the state read throws"
    }
  ]
}
