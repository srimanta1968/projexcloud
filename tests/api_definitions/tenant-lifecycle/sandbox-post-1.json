{
  "name": "POST /api/tenant-lifecycle/sandbox — sdk-tenant-lifecycle",
  "endpoint": "/api/tenant-lifecycle/sandbox",
  "method": "POST",
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/register"
  ],
  "expectedStatus": 201,
  "sdk": "sdk-tenant-lifecycle",
  "sourceFile": "packages/sdk-tenant-lifecycle/src/server/routes.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Creates a sandbox sub-tenant from the caller's JWT tenant_id (403 if the token carries no tenant_id) and provisions a sub-pool via createSandboxTenant; needs a real parent tenant + pool. Body fields (expires_at, sanitization_policy) are optional.",
  "testCases": [
    {
      "name": "Create sandbox sub-tenant",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "expires_at": "{{static:2027-01-01T00:00:00Z}}",
        "sanitization_policy": "{{static:default-pii-mask}}"
      },
      "expectedStatus": 201
    }
  ]
}
