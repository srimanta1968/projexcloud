{
  "name": "POST /api/memberships - Create Tenant Membership (L3)",
  "endpoint": "/api/memberships",
  "sdk": "sdk-persona",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "c0fb5ae9-73b6-498e-9f9e-426edd3d9755",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "b04da8c5-aa75-47ec-9bd6-63df4200a351",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 25,
  "testability": "auto",
  "sourceFile": "packages/sdk-persona/src/server/routes.ts",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "POST /api/app-identities"
  ],
  "testCases": [
    {
      "name": "Create membership",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "app_identity_id": "{{cache:app-identities.create.response.data.app_identity.app_identity_id}}",
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "expectedStatus": 201
    }
  ],
  "captureResponse": {
    "memberships.create.response.data.membership.membership_id": "data.membership.membership_id",
    "memberships.create.response.data.membership.status": "data.membership.status"
  },
  "description": "Creates an L3 membership joining an existing L2 app_identity to a tenant, returning 201 with the new row. Requires a valid tenant JWT (requireAuth). Edge cases: only presence of app_identity_id and tenant_id is validated - there is no UUID-format check, no existence check and no comparison against the caller JWT tenant, so a caller can create a membership in a tenant they do not belong to; empty strings count as missing and yield 400; a non-existent app_identity_id or tenant_id passes validation and then trips a foreign-key violation inside an untried service call, which the Fastify default error handler reports as 500 rather than 404 or 409; a malformed non-UUID id likewise becomes a 500; the endpoint is not idempotent - repeating the same pair either duplicates the membership or trips a unique constraint (again a 500) depending on the schema.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "missing fields",
      "when": "app_identity_id or tenant_id is absent or an empty string"
    },
    {
      "status": 500,
      "code": "InternalServerError",
      "message": "Internal Server Error",
      "when": "createMembership throws and the route has no try/catch, so the Fastify default error handler responds - a non-existent app_identity_id/tenant_id foreign key, a non-UUID id failing the uuid cast, a duplicate unique-constraint violation, or any DB error"
    }
  ]
}
