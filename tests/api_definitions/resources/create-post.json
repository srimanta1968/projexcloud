{
  "name": "POST /api/resources - register an owned infra resource",
  "endpoint": "/api/resources",
  "sdk": "sdk-resource-registry",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "317eb69b-9b28-4831-a9d6-fa823384b2c6",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "featureId": "a5e2bb64-87b9-4f98-abb8-383750f3b56b",
  "epicId": "1f467f44-ec1c-4e5e-86f7-c5e33b6b58b6",
  "priority": 30,
  "dependsOn": [
    "POST /api/auth/register"
  ],
  "captureResponse": {
    "resources.create.response.data.resource.resource_id": "data.resource.resource_id"
  },
  "fieldEnums": {
    "environment": [
      "dev",
      "staging",
      "prod"
    ],
    "status": [
      "registered",
      "quarantined"
    ]
  },
  "testCases": [
    {
      "name": "Register an owned resource with owner + approver",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "resource_id": "{{var:resource_id}}",
        "resource_type": "s3_bucket",
        "environment": "dev",
        "owner": "platform-team",
        "approved_by": "cto",
        "team": "platform",
        "repo": "org/infra-platform",
        "terraform_module": "modules/s3-bucket",
        "cloud_account": "123456789012",
        "cost_center": "CC-1001",
        "data_classification": "internal",
        "network_zone": "private",
        "created_by": "gitops-sync",
        "expires_at": "{{dynamic:futuredatetime}}"
      },
      "expectedStatus": 201
    }
  ],
  "expectedResponse": {
    "data": {
      "resource": {
        "resource_id": "string",
        "owner": "string",
        "approved_by": "string"
      }
    }
  },
  "description": "Registers or updates an owned resource in the ownership registry, driven by GitOps sync, returning 201 with the stored record. Requires a valid tenant JWT (requireAuth). Edge cases: five fields are mandatory presence checks (resource_id, resource_type, environment, owner, approved_by) and any one missing or empty produces the same single 400 message listing all five; values are not validated against any enum in the route, so an unknown resource_type or a typo in environment is accepted and stored; because this is a sync endpoint the same resource_id is expected to be re-posted - the upsert makes it effectively idempotent on resource_id with later fields overwriting earlier ones, so a repeat call is a 201 rather than a 409; the caller JWT tenant is neither recorded nor checked, so ownership rows are platform-wide; a constraint or DB failure is a 500 that echoes the underlying error message in details[].",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "resource_id, resource_type, environment, owner, approved_by are required",
      "when": "any of resource_id, resource_type, environment, owner or approved_by is absent or empty"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "registerResource throws - a constraint violation or any DB error; the underlying message is echoed in details[]"
    }
  ]
}
