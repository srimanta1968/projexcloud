{
  "name": "GET /api/resources - list/filter ownership records",
  "endpoint": "/api/resources",
  "sdk": "sdk-resource-registry",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "317eb69b-9b28-4831-a9d6-fa823384b2c6",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "featureId": "a5e2bb64-87b9-4f98-abb8-383750f3b56b",
  "epicId": "1f467f44-ec1c-4e5e-86f7-c5e33b6b58b6",
  "priority": 36,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/resources"
  ],
  "fieldEnums": {
    "status": [
      "registered",
      "quarantined"
    ]
  },
  "testCases": [
    {
      "name": "List ownership records (registered) after creating one",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "queryParams": {
        "status": "registered",
        "limit": "50"
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "resources": []
    }
  },
  "description": "Lists resource-ownership registry records for the admin \"who owns what\" view, filterable by owner, status, environment, resource_type and team with limit/offset paging (P10/E5). Requires a valid tenant JWT (requireAuth). Edge cases: every filter is optional, so an unfiltered call returns the whole registry - this is a platform-wide registry and the caller JWT tenant is never applied as a filter, so tenant scoping is not enforced on this route; limit and offset are passed through parseInt, so a non-numeric or empty value becomes NaN and surfaces as a 500 InternalError rather than a 400; an unknown status or owner value simply matches nothing and returns 200 with an empty resources array, never a 404; filters combine with AND, so contradictory filters yield an empty list.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "listResources throws - NaN limit/offset from a non-numeric query value, or any DB error"
    }
  ]
}
