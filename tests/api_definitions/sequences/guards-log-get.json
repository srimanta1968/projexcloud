{
  "name": "GET /api/sequences/guards/log - Guard decision audit log",
  "endpoint": "/api/sequences/guards/log",
  "sdk": "sdk-sequence",
  "sourceFile": "packages/sdk-sequence/src/server/routes.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "8265f9aa-fbaf-4126-9c75-9aef4b054f42",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "priority": 49,
  "testability": "auto",
  "description": "List the guard decision audit trail for a tenant, newest first. Each entry records the decision (allow/block), the reason (cooldown/max_messages/duplicate/circuit_open), subject, channel and any dedupe_hash. Tenant-scoped via the required tenant_id query param; optional decision filter (allow|block) and limit. Returns an empty array when no guard checks have run.",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "fieldEnums": {
    "decision": ["allow", "block"]
  },
  "testCases": [
    {
      "name": "List the tenant's guard log (empty is valid)",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "queryParams": { "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}" },
      "expectedStatus": 200,
      "expectedResponse": { "data": { "entries": [] } }
    }
  ],
  "errorCases": [
    { "status": 400, "code": "ValidationError", "message": "tenant_id query param required", "when": "tenant_id query param missing" }
  ]
}
