{
  "name": "POST /api/mcp/server-registrations/:id/disable — sdk-mcp-bridge",
  "endpoint": "/api/mcp/server-registrations/:id/disable",
  "method": "POST",
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/mcp/server-registrations"
  ],
  "expectedStatus": 200,
  "sdk": "sdk-mcp-bridge",
  "sourceFile": "packages/sdk-mcp-bridge/src/server/routes.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Disable targets an existing MCP server registration whose creation (POST /api/mcp/server-registrations) requires a live MCP fixture and is itself manual; the registration_id cannot be captured in an automated run. Required body field (reason) filled.",
  "testCases": [
    {
      "name": "Disable a registered MCP server",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "id": "{{cache:mcp.server-registrations.create.response.data.registration_id}}"
      },
      "payload": {
        "reason": "{{static:Decommissioned by automated test}}"
      },
      "expectedStatus": 200
    }
  ]
}
