{
  "name": "PUT /api/notifications/frequency-policy — tenant cap + no-answer dedup window",
  "endpoint": "/api/notifications/frequency-policy",
  "sdk": "sdk-notification",
  "sourceFile": "packages/sdk-notification/src/server/routes.ts",
  "method": "PUT",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "9d3c7f66-6f78-422f-955f-eda4ad024295",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "featureId": "85023098-3515-4f3a-8163-84a74c5ff8f3",
  "epicId": "bd0b5996-171f-46f2-8ead-0b72593f97df",
  "priority": 60,
  "testability": "auto",
  "dependsOn": ["POST /api/auth/signup-tenant"],
  "coversCriterion": [
    "9d3c7f66-6f78-422f-955f-eda4ad024295#AC1",
    "9d3c7f66-6f78-422f-955f-eda4ad024295#AC4"
  ],
  "description": "Upserts a tenant's frequency policy for one channel + purpose (either may be '*', the catch-all). An upsert, so 200 rather than 201. Proves AC1 and AC4. Two distinct settings, deliberately not merged: max_per_day is a RATE answered by counting, and dedup_window_seconds is an IDENTITY answered by a unique key — counting cannot express dedup (two retries are two rows, so they would burn two units of the cap and still both be delivered) and a unique key cannot express a rate, so conflating them produces a system that is wrong in one direction or the other. Caps are evaluated per channel AND per purpose (AC4), so an OTP is never throttled because marketing exhausted the allowance. QA edge cases: max_per_day null means UNCAPPED and is explicitly NOT the same as 0, which blocks everything — the shipped platform default is uncapped precisely because turning caps on for every existing tenant during a migration would silently start dropping their traffic, the one outcome an additive change must not cause; a negative max_per_day or a dedup window outside 0..604800 (7 days) is a 400; the PLATFORM row (tenant_id NULL) is unreachable from this endpoint, since a tenant editing the shared default would change every other tenant's limits; precedence on read is tenant before platform and then most-specific-first, but a tenant catch-all still beats a platform-specific rule because the tenant has stated a house policy.",
  "testCases": [
    {
      "name": "Cap marketing SMS at 5 a day with a 15-minute dedup window",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "channel": "sms",
        "purpose": "marketing",
        "max_per_day": 5,
        "dedup_window_seconds": 900,
        "updated_by": "api-regression"
      },
      "expectedStatus": 200
    },
    {
      "name": "Leave transactional email uncapped but still deduped",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "channel": "email",
        "purpose": "transactional",
        "max_per_day": null,
        "dedup_window_seconds": 300
      },
      "expectedStatus": 200
    }
  ],
  "errorCases": [
    {
      "name": "A negative cap is refused",
      "status": 400,
      "code": "VALIDATION_ERROR",
      "message": "max_per_day must be a non-negative integer, or null for uncapped",
      "when": "max_per_day is negative or not an integer; null is accepted and means uncapped",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "channel": "sms",
        "max_per_day": -1
      },
      "expectedStatus": 400,
      "expectedError": { "code": "VALIDATION_ERROR" }
    },
    {
      "name": "A dedup window beyond seven days is refused",
      "status": 400,
      "code": "VALIDATION_ERROR",
      "message": "dedup_window_seconds must be an integer between 0 and 604800 (7 days)",
      "when": "dedup_window_seconds is negative or exceeds 604800",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "channel": "sms",
        "dedup_window_seconds": 999999
      },
      "expectedStatus": 400,
      "expectedError": { "code": "VALIDATION_ERROR" }
    },
    {
      "name": "Missing tenant_id is refused",
      "status": 400,
      "code": "VALIDATION_ERROR",
      "message": "tenant_id is required",
      "when": "tenant_id is absent, so the policy could not be scoped to a tenant",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "payload": { "channel": "sms", "max_per_day": 5 },
      "expectedStatus": 400,
      "expectedError": { "code": "VALIDATION_ERROR" }
    },
    {
      "name": "Unauthenticated is refused by the default-deny gate",
      "status": 401,
      "code": "Unauthorized",
      "message": "missing or invalid tenant token",
      "when": "no valid tenant Bearer token or tenant-scoped API key on the request",
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "channel": "sms",
        "max_per_day": 5
      },
      "expectedStatus": 401
    }
  ]
}
