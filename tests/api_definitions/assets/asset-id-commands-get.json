{
  "name": "GET /api/assets/:asset_id/commands — list commands for an asset",
  "endpoint": "/api/assets/:asset_id/commands",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "16d543f8-4b54-44a9-b3d6-a3fa7ddfa83d",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "priority": 45,
  "dependsOn": ["POST /api/auth/signup-tenant", "POST /api/assets"],
  "testability": "manual",
  "skipReason": "Needs a real sub-bearing JWT + a registered asset. Lists recent commands issued to the asset.",
  "testCases": [
    {
      "name": "List commands for an asset",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "pathParams": { "asset_id": "{{cache:assets.create.response.data.asset_id}}" },
      "expectedStatus": 200,
      "testability": "manual"
    }
  ]
}
