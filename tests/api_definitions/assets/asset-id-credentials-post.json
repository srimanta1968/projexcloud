{
  "name": "POST /api/assets/:asset_id/credentials — mint a per-robot scoped credential",
  "endpoint": "/api/assets/:asset_id/credentials",
  "sdk": "sdk-asset",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "30f36b30-ef98-4d15-a631-19ae61939db2",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "priority": 43,
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/assets"
  ],
  "testability": "manual",
  "skipReason": "Needs a real sub-bearing JWT + a registered asset_id (P12 asset chain). Returns a plaintext API key (secret, shown once) scoped to the asset via sdk-api-keys.",
  "captureResponse": {
    "assets.credential.response.data.key_id": "data.key_id"
  },
  "testCases": [
    {
      "name": "Mint a credential scoped to the robot",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "asset_id": "{{cache:assets.create.response.data.asset_id}}"
      },
      "payload": {
        "rate_limit_rpm": 600
      },
      "expectedStatus": 201,
      "testability": "manual"
    }
  ]
}
