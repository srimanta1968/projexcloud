{
  "name": "POST /api/hdk-idp/offline-auth/log - Sync offline-auth event",
  "endpoint": "/api/hdk-idp/offline-auth/log",
  "sdk": "hdk-idp",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "20d05b4b-b543-4b2d-b1e3-c230df085ecb",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "c6a628f9-6d3a-4b36-b075-a3269e98833c",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 50,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/hdk-idp/claims"
  ],
  "fieldEnums": {
    "method": [
      "biometric",
      "pin",
      "passkey"
    ]
  },
  "captureResponse": {
    "offline-auth.log.response.data.log.log_id": "data.log.log_id",
    "offline-auth.log.response.data.log.device_uuid": "data.log.device_uuid",
    "offline-auth.log.response.data.log.person_id": "data.log.person_id"
  },
  "testCases": [
    {
      "name": "Sync biometric offline-auth",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "device_uuid": "{{cache:claims.register.response.data.claim.device_uuid}}",
        "person_id": "{{cache:claims.register.response.data.claim.person_id}}",
        "method": "biometric",
        "occurred_at": "{{dynamic:pastdatetime}}"
      },
      "expectedStatus": 201
    }
  ],
  "description": "Records an authentication a device performed while offline, writing it to hdk_idp.offline_auth_log on the next sync and touching last_used_at on the matching (device_uuid, person_id) device_claim. All four fields - device_uuid, person_id, method and occurred_at - are required, and method must be one of biometric, pin or passkey (also enforced by a CHECK constraint on the table). Edge cases: an unrecognised method has its own distinct 400 branch separate from the missing-fields branch; there is no FK and no uniqueness constraint, so an unknown device/person is accepted and replaying the same event inserts a duplicate log row (not idempotent); when no claim matches, the last_used_at UPDATE affects zero rows silently and the call still returns 201; person_id must be a UUID and occurred_at must be a parseable timestamp - both are handled at the database and fail there, not in a validation branch.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in \"Bearer <token>\" form (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past) (requireAuth preHandler)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "missing fields",
      "when": "device_uuid, person_id, method or occurred_at missing/empty in the body"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "invalid method",
      "when": "method is present but not one of 'biometric', 'pin', 'passkey'"
    },
    {
      "status": 400,
      "code": "FST_ERR_CTP_EMPTY_JSON_BODY",
      "message": "Body cannot be empty when content-type is set to application/json",
      "when": "Content-Type: application/json declared but the request body is empty or unparseable (Fastify JSON body parser, before the handler runs)"
    },
    {
      "status": 500,
      "code": "InternalServerError",
      "message": "Internal Server Error",
      "when": "logOfflineAuth INSERT fails - person_id is not a valid UUID (hdk_idp.offline_auth_log.person_id is UUID NOT NULL), occurred_at is not a parseable date (Invalid Date), or the Postgres pool is unavailable"
    }
  ]
}
