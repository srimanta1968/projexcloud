{
  "name": "POST /api/campaigns — sdk-campaign",
  "endpoint": "/api/campaigns",
  "method": "POST",
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "expectedStatus": 201,
  "sdk": "sdk-campaign",
  "sourceFile": "packages/sdk-campaign/src/server/routes.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Needs a real sub-bearing JWT from the signup-tenant chain (tenant_id + token). Handler requires tenant_id + name; creates campaign.campaign and returns 201.",
  "captureResponse": {
    "campaigns.create.response.data.campaign.campaign_id": "data.campaign.campaign_id"
  },
  "testCases": [
    {
      "name": "Create a marketing campaign for the signed-up tenant",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "name": "{{dynamic:name}}",
        "variant_flag_id": "{{static:flag_homepage_v2}}"
      },
      "expectedStatus": 201
    }
  ]
}
