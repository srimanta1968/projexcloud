{
  "name": "POST /api/consents - Grant consent receipt",
  "endpoint": "/api/consents",
  "sdk": "sdk-consent",
  "sourceFile": "packages/sdk-consent/src/server/routes.ts",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "23e995f5-e424-4525-8748-64937ca3181b",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "29651845-09ac-4f92-8964-35792daf8a86",
  "epicId": "00996d23-f667-4a82-9285-7728ba357e7b",
  "priority": 40,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "POST /api/consents/purposes"
  ],
  "testCases": [
    {
      "name": "Grant consent for the registered purpose",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "person_id": "{{cache:auth.register.response.data.userId}}",
        "purpose_id": "{{cache:consents.purposes.create.response.data.purpose.purpose_id}}",
        "processor": "tenant",
        "app_id": "healthcare",
        "jurisdiction": "US-CA",
        "granted_by_actor": "{{cache:auth.register.response.data.userId}}",
        "expires_at": "{{dynamic:futuredatetime}}",
        "source_tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "target_tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "expectedStatus": 201
    }
  ],
  "statusTransitions": {
    "entity": "consent.receipt",
    "field": "revoked_at",
    "flow": [
      "active",
      "revoked"
    ],
    "transitions": [
      {
        "from": "(none)",
        "to": "active",
        "via": "POST /api/consents"
      },
      {
        "from": "active",
        "to": "revoked",
        "via": "POST /api/consents/:receipt_id/revoke"
      }
    ]
  },
  "expectedResponse": {
    "data": {
      "receipt": {
        "receipt_id": "string",
        "person_id": "string",
        "purpose_id": "string"
      }
    }
  },
  "captureResponse": {
    "consents.create.response.data.receipt.receipt_id": "data.receipt.receipt_id"
  },
  "description": "Grants a new consent receipt for the (person, purpose, processor, jurisdiction) tuple (FR-CNS-1) and returns 201 with the receipt. person_id, purpose_id, processor, app_id, jurisdiction and granted_by_actor are all mandatory non-empty strings; expires_at is optional but must parse as ISO-8601 when supplied; source_tenant_id and target_tenant_id are optional and drive the cross-border check. Edge cases: a cross-border transfer refused on jurisdictional grounds returns HTTP 451 CrossBorderViolation (FR-CNS-5), not a 403; an existing active receipt for the same tuple is a 409, so granting twice is not idempotent - revoke first; a purpose_id that is not in the registry trips a foreign-key violation that is mapped to a 400 rather than a 404; an expires_at in the past is accepted (only parseability is validated); requires a valid JWT.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification or is expired (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "body must be an object",
      "when": "Request body is absent or not a JSON object"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "<field> is required",
      "when": "Any of person_id, purpose_id, processor, app_id, jurisdiction, granted_by_actor is absent or whitespace-only"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "expires_at must be ISO-8601",
      "when": "expires_at is supplied but Date.parse cannot parse it"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "purpose_id does not exist",
      "when": "The receipt insert raises a foreign-key violation - purpose_id has not been registered via POST /api/consents/purposes"
    },
    {
      "status": 409,
      "code": "Conflict",
      "message": "Active receipt already exists for this tuple",
      "when": "A duplicate-key violation - an active receipt already covers this (person, purpose, processor, jurisdiction) tuple"
    },
    {
      "status": 451,
      "code": "CrossBorderViolation",
      "message": "<CrossBorderError message>",
      "when": "grantConsent throws CrossBorderError - the source_tenant_id/target_tenant_id transfer is not permitted for this jurisdiction (FR-CNS-5)"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "Any other grantConsent failure (database error, event-emit failure)"
    }
  ]
}
