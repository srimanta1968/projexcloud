{
  "name": "POST /api/consents/check - Check consent state",
  "endpoint": "/api/consents/check",
  "sdk": "sdk-consent",
  "sourceFile": "packages/sdk-consent/src/server/routes.ts",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "32cf6258-4ee4-42cf-a369-390405744092",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "29651845-09ac-4f92-8964-35792daf8a86",
  "epicId": "00996d23-f667-4a82-9285-7728ba357e7b",
  "priority": 50,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/consents/purposes",
    "POST /api/consents"
  ],
  "testCases": [
    {
      "name": "Check the granted receipt is active",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "person_id": "{{cache:auth.register.response.data.userId}}",
        "purpose_id": "{{cache:consents.purposes.create.response.data.purpose.purpose_id}}",
        "processor": "tenant",
        "jurisdiction": "US-CA"
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "granted": "boolean",
      "receipt_id": "string"
    }
  },
  "captureResponse": {
    "consents.check.response.data.receipt_id": "data.receipt_id"
  },
  "description": "Returns the current consent state for a (person_id, purpose_id, processor, jurisdiction) tuple. This is the hot-path gate every downstream SDK calls before processing PII (FR-CNS-1, FR-CNS-5). Always returns 200 with the evaluated result - a tuple with no receipt, an expired receipt or a revoked receipt is reported as not-allowed in the payload rather than as a 404 or 403. All four fields are mandatory non-empty strings and are trimmed. Edge cases: whitespace-only values count as missing; an unknown person_id or purpose_id yields a negative result, not an error; the call is read-only, side-effect-free and safely repeatable; requires a valid JWT.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification or is expired (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "body must be an object",
      "when": "Request body is absent or not a JSON object"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "<field> is required",
      "when": "Any of person_id, purpose_id, processor, jurisdiction is absent or whitespace-only"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "checkConsent throws (database unavailable or query error)"
    }
  ]
}
