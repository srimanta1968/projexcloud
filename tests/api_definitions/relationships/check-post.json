{
  "name": "POST /api/relationships/check - ReBAC decision",
  "endpoint": "/api/relationships/check",
  "sdk": "sdk-rebac",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "89f91438-baa4-4532-bc99-c0c9ef118100",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "7efe9f11-7029-470a-abd6-fe506151fb65",
  "epicId": "00996d23-f667-4a82-9285-7728ba357e7b",
  "priority": 40,
  "testability": "auto",
  "sourceFile": "packages/sdk-rebac/src/server/routes.ts",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/app-identities",
    "POST /api/memberships",
    "POST /api/personas"
  ],
  "testCases": [
    {
      "name": "Check care-team reachability with default budget",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "subject_persona_id": "{{cache:personas.create.response.data.persona.persona_id}}",
        "target_persona_id": "{{cache:personas.create.response.data.persona.persona_id}}",
        "kind": "care-team",
        "budget": {
          "depth_cap": 4,
          "visit_cap": 1024
        }
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "decision": "string",
      "reason": "string",
      "traversal_depth": "number",
      "budget_used": {
        "visits": "number",
        "depth": "number"
      },
      "cached": "boolean"
    }
  },
  "captureResponse": {
    "relationships.check.response.data.decision": "data.decision",
    "relationships.check.response.data.traversal_depth": "data.traversal_depth"
  },
  "description": "Evaluates whether a subject_persona_id can reach a target_persona_id via a relationship of the given kind, running a bounded graph traversal and returning a decision plus budget usage. Requires subject_persona_id, target_persona_id and kind (all trimmed). Optional budget object, if present, MUST have numeric depth_cap and visit_cap. All validation failures collect into one 400 whose details[] lists every failing field; a traversal/DB failure returns 500.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "details[] lists failing fields: subject_persona_id/target_persona_id/kind required, or budget must have numeric depth_cap and visit_cap, or body must be an object",
      "when": "body not an object, a required field empty/missing, or a partial/non-numeric budget"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "checkRelationship() service throws or an error escapes the controller"
    }
  ]
}
