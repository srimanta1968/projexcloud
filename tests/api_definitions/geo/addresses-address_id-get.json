{
  "name": "GET /api/geo/addresses/:address_id - Read canonical address",
  "endpoint": "/api/geo/addresses/:address_id",
  "sdk": "sdk-geo",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "610809fe-0b1d-44e8-8e63-faa200e605d9",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "a135ad4d-61af-40c2-ac4d-a5b4f0a6ae55",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 40,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/geo/canonicalize"
  ],
  "testCases": [
    {
      "name": "Read by canonical id",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "address_id": "{{cache:geo.canonicalize.response.data.address.address_id}}"
      },
      "expectedStatus": 200
    }
  ],
  "description": "Reads one canonical address row by address_id and returns 200 with { data: { address } }. Read-only, requires a valid JWT; the record is not tenant-scoped (geo.address is a platform-wide canonical table), so any authenticated caller can read any address_id. Edge cases: unknown but well-formed UUID returns 404, an address_id that was consumed as the loser of POST /api/geo/merge is deleted and therefore also 404s afterwards, and a non-UUID address_id reaches the Postgres UUID cast unguarded (22P02) and surfaces as a 500.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification or is expired (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "NotFound",
      "when": "No geo.address row exists for the supplied address_id (never created, or deleted as the loser of a merge)"
    },
    {
      "status": 500,
      "code": "Internal Server Error",
      "message": "invalid input syntax for type uuid",
      "when": "address_id is not a valid UUID - the handler has no format guard, so the Postgres 22P02 error escapes as an unhandled 500"
    }
  ]
}
