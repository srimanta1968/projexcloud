{
  "name": "GET /api/voice/calls - List the tenant's calls",
  "endpoint": "/api/voice/calls",
  "sdk": "connector-twilio-voice",
  "sourceFile": "packages/connector-twilio-voice/src/server/routes.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "a89d6c67-d5dd-4933-9148-db8e91b4493a",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "priority": 63,
  "testability": "auto",
  "description": "List the tenant's call legs, newest first, optionally filtered by status, direction, or is_voicemail. is_voicemail=true isolates the calls that answering-machine detection classified as reaching voicemail rather than a person. Edge cases: 400 when the tenant_id query param is missing; a tenant with no calls returns an empty array rather than 404; is_voicemail is only applied when the param is present (absent means 'either'), and any value other than the literal 'true' reads as false; limit defaults to 50 and offset to 0.",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/voice/tracking-numbers",
    "POST /api/voice/calls"
  ],
  "fieldEnums": {
    "status": ["queued", "initiated", "ringing", "in-progress", "completed", "busy", "no-answer", "canceled", "failed"],
    "direction": ["inbound", "outbound"]
  },
  "testCases": [
    {
      "name": "List outbound calls for the tenant",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "queryParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "direction": "outbound",
        "limit": "50",
        "offset": "0"
      },
      "expectedStatus": 200,
      "expectedResponse": {
        "data": { "calls": [{ "voice_call_id": "string", "external_id": "string", "status": "string" }] }
      }
    }
  ],
  "errorCases": [
    { "status": 400, "code": "ValidationError", "message": "tenant_id query param required", "when": "tenant_id query param missing" },
    { "status": 401, "code": "Unauthorized", "message": "Unauthorized", "when": "Authorization bearer token missing or invalid" }
  ]
}
