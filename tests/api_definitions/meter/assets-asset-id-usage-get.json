{
  "name": "GET /api/meter/assets/:asset_id/usage — per-robot metered usage rollup",
  "endpoint": "/api/meter/assets/:asset_id/usage",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "62ece5ad-8f83-4321-bf0d-3535c59a80d2",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "priority": 34,
  "dependsOn": ["POST /api/auth/signup-tenant", "POST /api/assets"],
  "testability": "manual",
  "skipReason": "Needs a real sub-bearing JWT (same as the rest of the P12 asset chain); returns the per-(sensor, day, sku) usage rollup for a registered robot (empty until usage is metered).",
  "testCases": [
    {
      "name": "Read per-robot metered usage",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "pathParams": { "asset_id": "{{cache:assets.create.response.data.asset_id}}" },
      "expectedStatus": 200,
      "testability": "manual"
    }
  ]
}
