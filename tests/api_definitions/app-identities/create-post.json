{
  "name": "POST /api/app-identities - Create App Identity (L2)",
  "endpoint": "/api/app-identities",
  "sdk": "sdk-persona",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "c0fb5ae9-73b6-498e-9f9e-426edd3d9755",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "b04da8c5-aa75-47ec-9bd6-63df4200a351",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 20,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant"
  ],
  "testCases": [
    {
      "name": "Create app_identity for person + app",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "person_id": "{{cache:auth.register.response.data.userId}}",
        "app_id": "{{cache:auth.signup-tenant.response.data.app_id}}"
      },
      "expectedStatus": 201
    }
  ],
  "captureResponse": {
    "app-identities.create.response.data.app_identity.app_identity_id": "data.app_identity.app_identity_id"
  },
  "description": "Creates an L2 app_identity binding a canonical person_id to an app_id, returning 201 with the new row. Requires a valid tenant JWT (requireAuth). Edge cases: only presence of person_id and app_id is validated - no UUID-format check, no existence check and no tenant check - so a non-existent person_id/app_id or a malformed UUID escapes validation and fails inside the service; the route has no try/catch, so that failure is answered by the Fastify default error handler as 500 rather than 404 or 400. Empty strings count as missing and yield 400. The caller JWT tenant is never consulted, so cross-tenant creation is not blocked here. The endpoint is not idempotent: repeating the same person_id+app_id either creates another row or trips a unique constraint (again a 500) depending on the schema.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "missing fields",
      "when": "person_id or app_id is absent or an empty string"
    },
    {
      "status": 500,
      "code": "InternalServerError",
      "message": "Internal Server Error",
      "when": "createAppIdentity throws and the route has no try/catch, so the Fastify default error handler responds - non-UUID person_id/app_id failing the uuid cast, an unsatisfied person/app foreign key, a duplicate unique-constraint violation, or any DB error"
    }
  ]
}
