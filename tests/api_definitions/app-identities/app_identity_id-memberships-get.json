{
  "name": "GET /api/app-identities/:app_identity_id/memberships - List memberships for app_identity",
  "endpoint": "/api/app-identities/:app_identity_id/memberships",
  "sdk": "sdk-persona",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "c0fb5ae9-73b6-498e-9f9e-426edd3d9755",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "b04da8c5-aa75-47ec-9bd6-63df4200a351",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 35,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "POST /api/app-identities"
  ],
  "testCases": [
    {
      "name": "List memberships",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "app_identity_id": "{{cache:app-identities.create.response.data.app_identity.app_identity_id}}"
      },
      "expectedStatus": 200
    }
  ],
  "description": "Lists the L3 memberships attached to one app_identity. Requires a valid tenant JWT (requireAuth). Edge cases: this is a list, so an unknown but well-formed app_identity_id returns 200 with an empty memberships array - never a 404; there is no limit/offset paging, so an app_identity with many memberships returns them all in one payload; the caller JWT tenant is not compared to the rows, so membership rows for other tenants are visible to any authenticated caller - test tenant scoping explicitly; a malformed non-UUID app_identity_id fails the uuid cast in an untried service call and surfaces as a Fastify 500.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 500,
      "code": "InternalServerError",
      "message": "Internal Server Error",
      "when": "listMembershipsForAppIdentity throws and the route has no try/catch, so the Fastify default error handler responds - chiefly a non-UUID app_identity_id failing the uuid cast, or any DB error"
    }
  ]
}
