{
  "name": "DELETE /api/ai-gateway/tenant-credentials/:binding_id — revoke",
  "endpoint": "/api/ai-gateway/tenant-credentials/:binding_id",
  "sdk": "sdk-ai-gateway",
  "sourceFile": "packages/sdk-ai-gateway/src/server/handlers/tenantCredentialController.ts",
  "method": "DELETE",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "535060b4-a157-40c0-b8fb-d73468b09216",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "4fd90c9b-ab74-41ae-a6f4-0cff5cb038c8",
  "epicId": "76ec75df-cf40-4336-b591-42019674d864",
  "priority": 22,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/ai-gateway/tenant-credentials"
  ],
  "statusTransitions": {
    "entity": "ai_gateway.tenant_provider_credential",
    "field": "status",
    "flow": [
      "active",
      "revoked"
    ],
    "transitions": [
      {
        "from": "active",
        "to": "revoked",
        "via": "DELETE /api/ai-gateway/tenant-credentials/:binding_id"
      }
    ]
  },
  "description": "Revokes an active tenant credential binding (FR-BYOK-6), requiring a human-readable reason of at least 6 characters after trimming — mirroring the CMEK BYOK revoke pattern so the audit ledger always carries a justification. QA edge cases: the reason guard trims first, so a body of six spaces is rejected as a 400 while 'typo!!' is accepted — assert the trim, not just the length; as with rotate, the 404-vs-500 split is decided by testing the thrown error message for the substring 'not found', so an unknown binding_id is a 404 and everything else is a 500; revoking an ALREADY-revoked binding is not idempotent-safe — the second call no longer matches an active row and returns 404 rather than 200, which is the repeat-call case to cover; revocation is a soft state change, so the binding row remains listable with a revoked status rather than disappearing; the important downstream effect is that completions for that (tenant, provider) fall back to the platform credential and start billing the markup SKU again, which is worth asserting end-to-end.",
  "testCases": [
    {
      "name": "Revoke an active binding with a reason",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "binding_id": "{{cache:ai-gateway-tenant-credentials.create.response.data.binding.binding_id}}"
      },
      "payload": {
        "reason": "{{static:rotating to a new provider account}}"
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "binding": {
        "binding_id": "string",
        "status": "string",
        "revoked_at": "string"
      }
    }
  },
  "errorCases": [
    {
      "status": 400,
      "code": "ValidationError",
      "message": "binding_id path param is required",
      "when": "the :binding_id path segment is empty"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "reason must be at least 6 characters",
      "when": "body.reason is absent, or has fewer than 6 characters after trimming (whitespace-only reasons are rejected)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "<error message containing \"not found\">",
      "when": "no active binding matches the binding_id — unknown id, or the binding was already revoked by an earlier call"
    },
    {
      "status": 500,
      "code": "RevokeFailed",
      "message": "<underlying error message> | revoke failed",
      "when": "revokeTenantCredential throws for any reason other than not-found — malformed binding_id UUID, audit/KMS failure, or a database error"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "no Authorization header, or it is not of the form 'Bearer <jwt>' (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "the bearer JWT fails verifyJwt — bad signature, wrong JWT_SECRET, or exp already elapsed (requireAuth preHandler)"
    }
  ]
}
