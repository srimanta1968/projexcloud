{
  "name": "GET /api/ai-gateway/tenant-credentials — list bindings",
  "endpoint": "/api/ai-gateway/tenant-credentials",
  "sdk": "sdk-ai-gateway",
  "sourceFile": "packages/sdk-ai-gateway/src/server/handlers/tenantCredentialController.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "0602a534-2253-4a1a-8c06-1a0cd92d50b9",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "4fd90c9b-ab74-41ae-a6f4-0cff5cb038c8",
  "epicId": "76ec75df-cf40-4336-b591-42019674d864",
  "priority": 19,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/ai-gateway/tenant-credentials"
  ],
  "description": "Lists a tenant's AI provider credential bindings for the tenant-admin UI. Each entry carries the binding lifecycle metadata plus last_4 of the key; credential_envelope is never returned, which is the primary security assertion — a response containing envelope bytes or a full key is a defect. QA edge cases: tenant_id is a REQUIRED query param and is read straight from the URL rather than from the caller's JWT claims, so an authenticated user of tenant A can list tenant B's bindings by changing the query string — that cross-tenant isolation gap is the most valuable test here; an unknown tenant_id returns 200 with an empty bindings array rather than a 404, so 'no such tenant' and 'tenant with no bindings' are indistinguishable; the listing is unpaginated with no limit/offset and no status filter, so revoked bindings are returned alongside active ones and must be filtered client-side; any database failure is flattened to a generic 500 'list failed' with the real cause only in the server log.",
  "testCases": [
    {
      "name": "List bindings for a tenant",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "expectedStatus": 200,
      "queryParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      }
    }
  ],
  "expectedResponse": {
    "data": {
      "bindings": "array"
    }
  },
  "errorCases": [
    {
      "status": 400,
      "code": "ValidationError",
      "message": "tenant_id query param is required",
      "when": "the tenant_id query param is absent or empty"
    },
    {
      "status": 500,
      "code": "ListFailed",
      "message": "list failed",
      "when": "listTenantCredentials throws — malformed tenant UUID or a database error; the underlying message is logged but never returned"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "no Authorization header, or it is not of the form 'Bearer <jwt>' (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "the bearer JWT fails verifyJwt — bad signature, wrong JWT_SECRET, or exp already elapsed (requireAuth preHandler)"
    }
  ]
}
