{
  "name": "GET /api/secrets/:ref — sdk-secrets",
  "endpoint": "/api/secrets/:ref",
  "method": "GET",
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/secrets"
  ],
  "expectedStatus": 200,
  "sdk": "sdk-secrets",
  "sourceFile": "packages/sdk-secrets/src/server/routes.ts",
  "testability": "auto",
  "testCases": [
    {
      "name": "Retrieve a previously registered SecretRef by ref",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "ref": "{{cache:secrets.store.response.data.ref}}"
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "ref": "string",
      "scope": "string",
      "provider": "string",
      "kms_key_id": "string",
      "created_at": "string",
      "rotated_at": "string"
    }
  },
  "description": "Looks up a SecretRef in the catalog by its ref string, which is URL-encoded in the path and decoded by the handler. Returns the catalog record (ref, scope, kms_key_id, rotation metadata) and never the secret material itself. Requires a valid tenant JWT (requireAuth). Edge cases: the ref path segment MUST be percent-encoded, since refs typically contain \"/\" and \":\" characters that would otherwise split into extra path segments and route-miss to a 404; a malformed percent sequence (for example a bare \"%\") makes decodeURIComponent throw a URIError, which is caught and reported as a 500 InternalError rather than a 400; an unknown but well-formed ref returns 404 NotFound with the decoded ref echoed in details[]; the caller JWT tenant is never compared to the record, so any authenticated caller can resolve any ref.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "No SecretRef registered for <ref>",
      "when": "retrieveSecret returns no record for the decoded ref"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "decodeURIComponent throws a URIError on a malformed percent-encoded ref, or retrieveSecret throws a DB error"
    }
  ]
}
