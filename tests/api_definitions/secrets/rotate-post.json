{
  "name": "POST /api/secrets/:ref/rotate - Rotate underlying KMS key",
  "endpoint": "/api/secrets/:ref/rotate",
  "sdk": "sdk-secrets",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "testability": "auto",
  "taskId": "507a8a8a-704d-451c-9134-a2ea293abcd6",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "36d7be6f-03a6-4d40-aa64-cd3552ef5683",
  "epicId": "ee93bf20-3149-45d5-a712-d55cc74df2d9",
  "testCases": [
    {
      "name": "Rotate previously registered SecretRef",
      "pathParams": {
        "ref": "secret%3A%2F%2Ftenant%2Fdev-test-key-001"
      },
      "payload": {},
      "expectedStatus": 200,
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      }
    }
  ],
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/secrets"
  ],
  "expectedResponse": {
    "data": {
      "ref": "object",
      "new_key_version": "string"
    }
  },
  "captureResponse": {
    "secrets.rotate.response.data.new_key_version": "data.new_key_version"
  },
  "description": "Rotates the KMS key version backing a registered SecretRef and stamps rotated_at on the catalog row, returning the rotation result. The ref is URL-encoded in the path and decoded by the handler. Requires a valid tenant JWT (requireAuth). Edge cases: no body is read, so any payload is ignored; the ref MUST be percent-encoded because refs typically contain \"/\" and \":\" which would otherwise split into extra path segments and route-miss to a 404, and a malformed percent sequence makes decodeURIComponent throw a URIError that is caught as a 500 rather than a 400; rotating an unregistered ref is a 404 NotFound, matched on the service message starting with \"Secret reference not registered\"; rotation is repeatable but not idempotent - each call mints a new key version and rewrites rotated_at, so callers must not retry blindly; a KMS-side failure is a 500 InternalError, indistinguishable in status from a DB failure.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "Secret reference not registered: <ref>",
      "when": "rotateSecret throws a message starting with \"Secret reference not registered\" - the decoded ref has no catalog row"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "decodeURIComponent throws a URIError on a malformed percent-encoded ref, or rotateSecret throws any other error - KMS rotation failure or a DB error"
    }
  ]
}
