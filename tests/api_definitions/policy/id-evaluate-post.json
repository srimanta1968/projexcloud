{
  "name": "POST /policy/:id/evaluate — semantic-service",
  "endpoint": "/policy/:id/evaluate",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "sdk": "semantic-service",
  "sourceFile": "services/semantic-service/src/routes.ts",
  "_generated": true,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /policy/register"
  ],
  "testability": "auto",
  "testCases": [
    {
      "name": "Evaluate an active semantic policy (happy path)",
      "pathParams": {
        "id": "{{cache:policy.register.response.data.policy_id}}"
      },
      "payload": {
        "subject_type": "Doctor",
        "subject_id": "{{var:subject_id}}",
        "action": "write",
        "resource_type": "Prescription",
        "resource_id": "{{var:resource_id}}",
        "active_edges": [
          {
            "kind": "care-team",
            "from_object_type": "Doctor",
            "to_object_type": "Patient",
            "to_object_id": "{{var:resource_id}}",
            "active": true
          }
        ],
        "trace_id": "{{var:trace_id}}"
      },
      "expectedStatus": 200,
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      }
    }
  ],
  "expectedResponse": {
    "data": {
      "policy_id": "string",
      "decision": "string",
      "reason": "string",
      "latency_ms": 0,
      "trace_id": "string"
    }
  },
  "captureResponse": {
    "policy.evaluate.response.data.policy_id": "data.policy_id",
    "policy.evaluate.response.data.decision": "data.decision",
    "policy.evaluate.response.data.trace_id": "data.trace_id"
  },
  "description": "Evaluates the semantic-service policy named by the :id path param against an evaluation context (subject_type, action, resource_type, trace_id). Mounted into the api-gateway from services/semantic-service, so the gateway default-deny authGate applies and a valid tenant JWT is required even though the handler declares no requireAuth. Edge cases: all four of subject_type, action, resource_type and trace_id are mandatory and any omission produces a single 400; a deny decision is still a 200 with the decision in the body, so only transport failures produce a non-200; note the catch is unconditional and maps EVERY service throw to 404, so an unknown policy id, a non-UUID id failing the uuid cast, an IQL evaluation error and a DB outage all return the same 404 with the raw message - a 500 is never returned from this route, which makes an infrastructure failure indistinguishable from a missing policy.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form - gateway default-deny authGate onRequest hook (AUTH_GATE_MODE=enforce)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification in the gateway authGate"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "subject_type, action, resource_type, trace_id required",
      "when": "any of subject_type, action, resource_type or trace_id is absent or empty"
    },
    {
      "status": 404,
      "code": "EvaluateFailed",
      "message": "<service error message>",
      "when": "evaluate throws for any reason - unknown or inactive policy id, a non-UUID id failing the uuid cast, an IQL evaluation error, or a DB error; the catch maps every throw to 404"
    }
  ]
}
