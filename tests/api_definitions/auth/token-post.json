{
  "sdk": "sdk-api-keys",
  "sourceFile": "packages/sdk-api-keys/src/services/credentialService.ts",
  "requiresAuth": false,
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "epicId": "783fc94b-6992-49a5-a492-74ee0450a92f",
  "testability": "auto",
  "name": "POST /api/auth/token - OAuth2 client_credentials grant",
  "endpoint": "/api/auth/token",
  "method": "POST",
  "expectedStatus": 200,
  "taskId": "a12dbd99-2c44-404d-b5ad-4395f93b7c89",
  "featureId": "f45b599e-69b8-41f7-808a-e4ba7374acd6",
  "priority": 25,
  "description": "RFC 6749 section 4.4. Exchanges an application credential (client_id is the application slug or id, client_secret is the pk_live_ or pk_test_ key) for a short-lived service JWT carrying the scopes, tenant and synthetic persona of the key, with actor.kind service so machine traffic stays distinguishable from a human in the audit trail. Public by necessity - the credential IS the request body - and never cached (Cache-Control no-store). The optional scope parameter may NARROW what the key holds and can never widen it. Invalid, revoked and expired credentials all answer invalid_client with identical wording, so probing cannot confirm that a client exists. Because a minted token cannot be revoked before it expires, its lifetime is the revocation delay and is capped at one hour.",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/applications",
    "POST /api/applications/:application_id/keys"
  ],
  "fieldEnums": {
    "grant_type": [
      "client_credentials"
    ],
    "token_type": [
      "Bearer"
    ],
    "error": [
      "invalid_request",
      "invalid_client",
      "invalid_scope",
      "unsupported_grant_type"
    ]
  },
  "testCases": [
    {
      "name": "Exchange a key for a service token",
      "payload": {
        "grant_type": "client_credentials",
        "client_id": "{{cache:applications.create.response.data.application.slug}}",
        "client_secret": "{{cache:applications.id-keys.response.data.plaintext}}"
      },
      "expectedStatus": 200,
      "expectedResponse": {
        "access_token": "string",
        "token_type": "string",
        "expires_in": "number",
        "scope": "string"
      }
    },
    {
      "name": "Narrow the token to a subset of what the key holds",
      "payload": {
        "grant_type": "client_credentials",
        "client_secret": "{{cache:applications.id-keys.response.data.plaintext}}",
        "scope": "sla.clock.read"
      },
      "expectedStatus": 200
    }
  ],
  "errorCases": [
    {
      "name": "A scope the key does not hold cannot be requested",
      "payload": {
        "grant_type": "client_credentials",
        "client_secret": "{{cache:applications.id-keys.response.data.plaintext}}",
        "scope": "crm.contact.write"
      },
      "expectedStatus": 400
    },
    {
      "name": "Any other grant type is unsupported",
      "payload": {
        "grant_type": "password",
        "client_secret": "{{cache:applications.id-keys.response.data.plaintext}}"
      },
      "expectedStatus": 400
    },
    {
      "name": "An unknown credential is indistinguishable from a revoked one",
      "payload": {
        "grant_type": "client_credentials",
        "client_secret": "pk_live_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
      },
      "expectedStatus": 401
    },
    {
      "name": "client_secret is required",
      "payload": {
        "grant_type": "client_credentials"
      },
      "expectedStatus": 400
    }
  ]
}
