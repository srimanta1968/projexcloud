{
  "name": "POST /api/api-keys/:key_id/revoke - Revoke API key",
  "endpoint": "/api/api-keys/:key_id/revoke",
  "sdk": "sdk-api-keys",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "649c02dd-6922-48af-a637-c88aea12d665",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "7a33ef6c-2dad-4c40-a8a2-f414c13e6c48",
  "epicId": "00996d23-f667-4a82-9285-7728ba357e7b",
  "priority": 60,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/api-keys"
  ],
  "testCases": [
    {
      "name": "Revoke the freshly-issued key",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "key_id": "{{cache:api-keys.create.response.data.key.key_id}}"
      },
      "payload": {},
      "expectedStatus": 200
    }
  ],
  "statusTransitions": {
    "entity": "api-key",
    "field": "status",
    "flow": [
      "active",
      "rotating",
      "revoked",
      "expired"
    ],
    "transitions": [
      {
        "from": "active",
        "to": "revoked",
        "via": "POST /api/api-keys/:key_id/revoke"
      },
      {
        "from": "rotating",
        "to": "revoked",
        "via": "POST /api/api-keys/:key_id/revoke"
      }
    ]
  },
  "expectedResponse": {
    "data": {
      "key": {
        "key_id": "string",
        "status": "string"
      }
    }
  },
  "captureResponse": {
    "api-keys.revoke.response.data.key.key_id": "data.key.key_id",
    "api-keys.revoke.response.data.key.status": "data.key.status"
  },
  "description": "Immediately revokes an API key by id so it stops authenticating, returning the updated key record. Edge cases: revokeKey only matches an ACTIVE key, so revoking a key that is already revoked returns 404 NotFound (\"No active key with id ...\") rather than a 200 no-op — the call is therefore not idempotent and a retry after a successful revoke will 404; an unknown key_id is likewise 404; a malformed (non-UUID) key_id fails the query cast and surfaces as a 500, since the route performs no id-format validation; there is no tenant ownership check against the JWT, so authorization rests on key ids being unguessable; revocation is immediate with no grace period, unlike rotate.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "No Authorization: Bearer <jwt> header on a requireAuth route"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "No active key with id <key_id>",
      "when": "No active key matches key_id — it does not exist, or it was already revoked"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "revokeKey throws — e.g. key_id is not a valid UUID and the query cast fails, or the database is unreachable"
    }
  ]
}
