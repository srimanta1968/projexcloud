{
  "name": "GET /api/evidence/capture — sdk-evidence",
  "endpoint": "/api/evidence/capture",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "sdk": "sdk-evidence",
  "sourceFile": "packages/sdk-evidence/src/server/index.ts",
  "_generated": true,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/encounters",
    "POST /api/evidence/capture"
  ],
  "testCases": [
    {
      "name": "List captures for an encounter",
      "queryParams": {
        "encounter_id": "{{cache:encounters.create.response.data.encounter.encounter_id}}"
      },
      "expectedStatus": 200,
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      }
    }
  ],
  "description": "Lists every evidence capture belonging to one encounter, selected by the required encounter_id query param. Gated by the gateway default-deny authGate, so a valid tenant JWT is required. Edge cases: encounter_id is mandatory and its absence (or an empty value) is a 400, since the route would otherwise scan unbounded; an unknown but well-formed encounter_id returns 200 with an empty data array rather than a 404; there is no limit/offset paging, so an encounter with many captures returns the whole set in one payload; the caller JWT tenant is not compared to the rows, so captures on another tenant encounter are readable - verify tenant scoping deliberately; a non-UUID encounter_id fails the uuid cast in an untried service call and surfaces as a Fastify 500, not a 400.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form - gateway default-deny authGate onRequest hook (AUTH_GATE_MODE=enforce)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification in the gateway authGate"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "encounter_id query param required",
      "when": "the encounter_id query param is absent or empty"
    },
    {
      "status": 500,
      "code": "InternalServerError",
      "message": "Internal Server Error",
      "when": "listCapturesForEncounter throws and the route has no try/catch, so the Fastify default error handler responds - chiefly a non-UUID encounter_id failing the uuid cast, or any DB error"
    }
  ]
}
