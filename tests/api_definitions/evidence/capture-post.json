{
  "name": "POST /api/evidence/capture \u2014 sdk-evidence",
  "endpoint": "/api/evidence/capture",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 201,
  "sdk": "sdk-evidence",
  "sourceFile": "packages/sdk-evidence/src/server/index.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Auto-generated from implemented route scan; payload/expectedResponse inferred from the handler and need review before enabling automated execution.",
  "testCases": [
    {
      "name": "Auto-generated from route scan (review payload before enabling)",
      "payload": {
        "tenant_id": "{{static:REPLACE_WITH_ID}}",
        "encounter_id": "{{static:REPLACE_WITH_ID}}",
        "capturer_persona_id": "{{static:REPLACE_WITH_ID}}",
        "device_uuid": "{{static:sample}}",
        "device_attestation_id": "{{static:REPLACE_WITH_ID}}",
        "raw_blob_id": "{{static:REPLACE_WITH_ID}}",
        "blob_checksum": "{{static:sample}}",
        "captured_at": "{{dynamic:futuredatetime}}",
        "lat": "{{static:sample}}",
        "lng": "{{static:sample}}",
        "altitude": "{{static:sample}}",
        "imu_signature": "{{static:sample}}",
        "consent_ref": "{{static:sample}}",
        "retention_class": "{{static:sample}}",
        "retention_expires_at": "{{dynamic:futuredatetime}}"
      },
      "expectedStatus": 201
    }
  ]
}