{
  "name": "GET /api/lead-scoring/models/:id — sdk-lead-scoring",
  "endpoint": "/api/lead-scoring/models/:id",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "sdk": "sdk-lead-scoring",
  "sourceFile": "packages/sdk-lead-scoring/src/server/index.ts",
  "_generated": true,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "POST /api/lead-scoring/models"
  ],
  "description": "Fetches one lead-scoring model by model_id, returning model_id, tenant_id, vertical, trained_at (ISO or null), feature_set and status. QA edge cases: an unknown model_id returns 404 'not found'; the query is not tenant-scoped, so any caller with a model_id reads that tenant's model metadata; trained_at is null for models still in 'training' status, so consumers must handle the null; the response contains the model row only — feature weights require the separate /weights endpoint.",
  "testCases": [
    {
      "name": "Fetch a model by id",
      "pathParams": {
        "id": "{{cache:lead-scoring.create.response.data.model_id}}"
      },
      "expectedStatus": 200,
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      }
    }
  ],
  "expectedResponse": {
    "data": {
      "model_id": "string",
      "tenant_id": "string",
      "vertical": "string",
      "trained_at": "string",
      "feature_set": "object",
      "status": "string"
    }
  },
  "errorCases": [
    {
      "status": 404,
      "code": "NotFound",
      "message": "not found",
      "when": "no lead_scoring.model row matches the :id path param"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "no Authorization header, or it is not of the form 'Bearer <jwt>' (api-gateway default-deny authGate, AUTH_GATE_MODE=enforce)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "the bearer JWT fails verifyJwt — bad signature, wrong JWT_SECRET, or exp already elapsed (api-gateway default-deny authGate, AUTH_GATE_MODE=enforce)"
    }
  ]
}
