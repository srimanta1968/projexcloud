{
  "name": "POST /api/lead-scoring/score — sdk-lead-scoring",
  "endpoint": "/api/lead-scoring/score",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "sdk-lead-scoring",
  "sourceFile": "packages/sdk-lead-scoring/src/server/index.ts",
  "_generated": true,
  "dependsOn": [
    "POST /api/lead-scoring/models"
  ],
  "testability": "manual",
  "skipReason": "scoreContact() looks up an ACTIVE model for the (tenant_id, vertical) pair via getActiveModel and throws (400) when none exists; the parent POST /api/lead-scoring/models def neither activates nor captures a model id, so the tenant_id FK is not obtainable in an automated run. Required body fields filled for documentation.",
  "testCases": [
    {
      "name": "Score a contact (happy path)",
      "payload": {
        "tenant_id": "{{cache:lead-scoring.models.create.response.data.tenant_id}}",
        "vertical": "{{static:solar}}",
        "contact_id": "{{dynamic:uuid}}",
        "trace_id": "{{dynamic:uuid}}"
      },
      "expectedStatus": 200
    }
  ]
}
