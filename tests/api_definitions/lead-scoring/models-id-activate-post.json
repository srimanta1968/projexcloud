{
  "name": "POST /api/lead-scoring/models/:id/activate — sdk-lead-scoring",
  "endpoint": "/api/lead-scoring/models/:id/activate",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "sdk-lead-scoring",
  "sourceFile": "packages/sdk-lead-scoring/src/server/index.ts",
  "_generated": true,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/lead-scoring/models"
  ],
  "statusTransitions": {
    "entity": "lead_scoring.model",
    "field": "status",
    "flow": ["training", "active", "retired"],
    "transitions": [
      { "from": "training", "to": "active", "via": "POST /api/lead-scoring/models/:id/activate" }
    ]
  },
  "testCases": [
    {
      "name": "Activate a model (flip status to active)",
      "pathParams": {
        "id": "{{cache:lead-scoring.create.response.data.model_id}}"
      },
      "payload": {},
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "model_id": "string",
      "tenant_id": "string",
      "vertical": "string",
      "trained_at": "string",
      "feature_set": "object",
      "status": "string"
    }
  },
  "captureResponse": {
    "lead-scoring.activate.response.data.model_id": "data.model_id",
    "lead-scoring.activate.response.data.status": "data.status"
  }
}
