{
  "name": "POST /api/lead-scoring/next-best-action — sdk-lead-scoring",
  "endpoint": "/api/lead-scoring/next-best-action",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "sdk-lead-scoring",
  "sourceFile": "packages/sdk-lead-scoring/src/server/index.ts",
  "_generated": true,
  "dependsOn": [
    "POST /api/lead-scoring/models"
  ],
  "testability": "manual",
  "skipReason": "Calls scoreContact() then nextBestAction(); scoreContact looks up an ACTIVE model for the (tenant_id, vertical) pair via getActiveModel and throws (400) when none exists. The parent POST /api/lead-scoring/models def neither activates nor captures a model id, so the tenant_id FK is not obtainable in an automated run. Required body fields filled for documentation.",
  "testCases": [
    {
      "name": "Score + recommend next best action (happy path)",
      "payload": {
        "tenant_id": "{{cache:lead-scoring.models.create.response.data.tenant_id}}",
        "vertical": "{{static:solar}}",
        "contact_id": "{{dynamic:uuid}}",
        "trace_id": "{{dynamic:uuid}}"
      },
      "expectedStatus": 200
    }
  ]
}
