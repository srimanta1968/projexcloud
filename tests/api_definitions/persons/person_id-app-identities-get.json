{
  "name": "GET /api/persons/:person_id/app-identities - List App Identities for a person",
  "endpoint": "/api/persons/:person_id/app-identities",
  "sdk": "sdk-persona",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "c0fb5ae9-73b6-498e-9f9e-426edd3d9755",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "b04da8c5-aa75-47ec-9bd6-63df4200a351",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 35,
  "dependsOn": [
    "POST /api/auth/register"
  ],
  "testCases": [
    {
      "name": "List app identities",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "person_id": "{{cache:auth.register.response.data.userId}}"
      },
      "expectedStatus": 200
    }
  ],
  "description": "Lists every L2 app identity linked to one L1 person from persona.app_identity - app_identity_id, person_id, app_id, status, merged_into_app_identity_id and created_at - ordered by created_at ascending. Edge cases: an unknown person_id returns 200 with app_identities: [] rather than 404, so a non-existent person is indistinguishable from a person with no apps; person_id is a UUID column, so a non-UUID path segment fails at the database and surfaces as a 500 rather than a 400; merged identities are NOT filtered out - rows carrying a non-null merged_into_app_identity_id are still returned and callers must follow the merge pointer themselves; the result is unpaginated and the query filters on person_id alone, so the caller's JWT tenant does not narrow the rows.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in \"Bearer <token>\" form (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past) (requireAuth preHandler)"
    },
    {
      "status": 500,
      "code": "InternalServerError",
      "message": "Internal Server Error",
      "when": "listAppIdentitiesForPerson SELECT fails - person_id is not a valid UUID (persona.app_identity.person_id is UUID NOT NULL) or the Postgres pool is unavailable; the route has no try/catch so the rejection becomes Fastify default 500"
    }
  ]
}
