{
  "name": "POST /api/hdk-sync/replay/start - Open replay batch from device",
  "endpoint": "/api/hdk-sync/replay/start",
  "sdk": "hdk-sync",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "07a04c67-09aa-4656-9ec7-84301089abeb",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "83a11862-0f2a-43be-b8bf-c2bec019500d",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 40,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "PUT /api/hdk-sync/event-type-policies"
  ],
  "testCases": [
    {
      "name": "Start replay with one envelope",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "device_uuid": "test-device-uuid-001",
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "envelopes": []
      },
      "expectedStatus": 201
    }
  ],
  "captureResponse": {
    "hdk-sync.replay-start.response.data.batch.batch_id": "data.batch.batch_id"
  },
  "description": "Opens a replay batch for one device: accepts the offline-queued envelopes a device accumulated and returns the created batch plus the subset that was rejected. Returns 201 even when EVERY envelope is rejected — inspect data.rejected rather than the status code. Edge cases: envelopes must be an array, but an EMPTY array is accepted and creates a zero-item batch; envelopes whose event_type has no registered policy come back in data.rejected instead of failing the request; batch size is not capped by the route, so payload size is bounded only by the gateway body limit; device_uuid and tenant_id come from the body, not the JWT, so tenant scoping is caller-asserted.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in 'Bearer <jwt>' form (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "JWT signature invalid, malformed, or exp has elapsed"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "missing fields",
      "when": "device_uuid or tenant_id is absent/empty, or envelopes is not an array"
    }
  ]
}
