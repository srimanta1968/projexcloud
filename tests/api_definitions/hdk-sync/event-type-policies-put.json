{
  "name": "PUT /api/hdk-sync/event-type-policies - Register event_type conflict policy (AC-13)",
  "endpoint": "/api/hdk-sync/event-type-policies",
  "sdk": "hdk-sync",
  "method": "PUT",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "07a04c67-09aa-4656-9ec7-84301089abeb",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "83a11862-0f2a-43be-b8bf-c2bec019500d",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 20,
  "dependsOn": [
    "POST /api/auth/register"
  ],
  "testCases": [
    {
      "name": "Register RGA-text policy for clinical notes",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "event_type": "clinical.note.edit.v1",
        "conflict_policy": "human-review",
        "strategy_detail": "human-review:dual-control",
        "retention_class": "regulated"
      },
      "expectedStatus": 200
    }
  ],
  "fieldEnums": {
    "conflict_policy": [
      "crdt",
      "lww",
      "merge",
      "event-sourcing",
      "human-review"
    ],
    "retention_class": [
      "transient",
      "operational",
      "regulated"
    ]
  },
  "captureResponse": {
    "hdk-sync.event-type-policies.create.response.data.policy.event_type": "data.policy.event_type"
  },
  "description": "Registers (upserts) the conflict-resolution policy for a sync event_type: which of crdt | lww | merge | event-sourcing | human-review applies when two device replays disagree, plus an optional free-text strategy_detail and a retention_class. Idempotent — re-PUTting the same event_type overwrites the policy and returns 200 (never 201), so it is safe to replay. Edge cases: an unknown conflict_policy or retention_class is rejected 400 by an explicit whitelist in the route; an empty-string event_type is treated as missing; strategy_detail and retention_class are optional; the registry is global (not tenant-scoped), so a policy registered by one tenant's operator governs every tenant's replays for that event_type.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in 'Bearer <jwt>' form (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "JWT signature invalid, malformed, or exp has elapsed"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "missing fields",
      "when": "event_type or conflict_policy is absent/empty"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "invalid conflict_policy",
      "when": "conflict_policy is not one of crdt, lww, merge, event-sourcing, human-review"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "invalid retention_class",
      "when": "retention_class was supplied but is not one of transient, operational, regulated"
    }
  ]
}
