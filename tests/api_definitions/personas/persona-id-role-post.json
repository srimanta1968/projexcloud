{
  "name": "POST /api/personas/:persona_id/role — api-gateway",
  "endpoint": "/api/personas/:persona_id/role",
  "sdk": "sdk-persona",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "sourceFile": "services/api-gateway/src/app.ts",
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/app-identities",
    "POST /api/memberships",
    "POST /api/personas",
    "POST /api/role-templates"
  ],
  "testCases": [
    {
      "name": "Set persona role label",
      "pathParams": {
        "persona_id": "{{cache:personas.create.response.data.persona.persona_id}}"
      },
      "payload": {
        "role": "{{cache:role-templates.create.response.data.role_template.role_template_id}}"
      },
      "expectedStatus": 200,
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      }
    }
  ],
  "captureResponse": {
    "personas.set-role.response.success": "success"
  },
  "description": "Sets a persona's primary role. IMPORTANT: persona.persona stores the primary role as a role_template_id UUID — there is no free-text role column — so the `role` body field must be a role_template_id and a human-readable label such as 'admin' is rejected 400. Edge cases: a missing/empty role is 400 'role required'; a non-UUID role is a 400 with a distinct message; a well-formed UUID matching no role_template row violates the FK and surfaces as 500; the UPDATE row count is not checked, so an unknown :persona_id still returns {success:true} — this endpoint can never report 404.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Gateway default-deny authGate (AUTH_GATE_MODE=enforce): path is not on the public allowlist and no bearer token was sent"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "authGate ran requireAuth and the JWT failed verification or had expired"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "role required",
      "when": "body.role is absent or empty"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "role must be a role_template_id (uuid)",
      "when": "body.role is present but does not match the UUID pattern (e.g. a role label was sent)"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "<postgres error text>",
      "when": ":persona_id is not a valid UUID, or role references a role_template_id that does not exist (FK violation)"
    }
  ]
}
