{
  "name": "POST /api/tenants/:tenant_id/sub-tenants - Create sub-tenant",
  "endpoint": "/api/tenants/:tenant_id/sub-tenants",
  "sdk": "sdk-tenant",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "df5db7ba-0b3f-4c70-96ff-9fad07030640",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "d3f526f1-243f-4b31-b538-aeffeddca534",
  "epicId": "00996d23-f667-4a82-9285-7728ba357e7b",
  "priority": 35,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "testCases": [
    {
      "name": "Create a sub-tenant that shares parent pool",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "payload": {
        "app_id": "{{cache:auth.signup-tenant.response.data.app_id}}",
        "display_name": "{{dynamic:name}}",
        "region": "us-east-1",
        "placement": "share",
        "isolation_tier": "S",
        "brand_domain": "sub.example.com"
      },
      "expectedStatus": 201
    }
  ],
  "fieldEnums": {
    "placement": [
      "share",
      "tier-p",
      "tier-g"
    ],
    "isolation_tier": [
      "S",
      "P",
      "G"
    ]
  },
  "captureResponse": {
    "tenants.create-sub-tenant.response.data.tenant_id": "data.tenant_id",
    "tenants.create-sub-tenant.response.data.parent_tenant_id": "data.parent_tenant_id"
  },
  "description": "Creates a sub-tenant beneath :tenant_id. app_id, display_name and region are required; placement (share | tier-p | tier-g) decides whether the child shares the parent's pool or gets a dedicated tier. Edge cases: placement is whitelist-checked only when supplied — omitting it is legal and defers to the service default; the PARENT :tenant_id is not validated by the handler, so an unknown or non-UUID parent surfaces as a foreign-key violation mapped to 400 ValidationError, not 404; duplicate natural keys map to 409 Conflict; nesting depth and per-parent sub-tenant counts are not capped at this layer.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in 'Bearer <jwt>' form (requireAuth preHandler)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "JWT signature invalid, malformed, or exp has elapsed"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "app_id is required / display_name is required / region is required / placement must be one of share, tier-p, tier-g",
      "when": "any validateCreateSubTenant check fails; all failures are returned together in details[]"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "<pg error: violates foreign key constraint ...>",
      "when": ":tenant_id (the parent), reseller_id or geo_node_id references a row that does not exist"
    },
    {
      "status": 409,
      "code": "Conflict",
      "message": "<pg error: duplicate key value violates unique constraint ...>",
      "when": "a unique constraint (e.g. brand_domain) is violated"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "<error text containing 'not found'>",
      "when": "the service throws a not-found error while resolving the parent tenant or a referenced entity"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "Unexpected service/DB failure (connection loss, or a constraint the handler does not map)"
    }
  ]
}
