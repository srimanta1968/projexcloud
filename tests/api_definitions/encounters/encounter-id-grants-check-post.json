{
  "name": "POST /api/encounters/:encounter_id/grants/check — sdk-engagement",
  "endpoint": "/api/encounters/:encounter_id/grants/check",
  "method": "POST",
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/encounters",
    "POST /api/personas"
  ],
  "expectedStatus": 200,
  "sdk": "sdk-engagement",
  "sourceFile": "packages/sdk-engagement/src/server/routes.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Requires an open encounter and a grantee persona; checkGrant evaluates scope.methods for the given method.",
  "testCases": [
    {
      "name": "Check whether a grantee may call a method",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "encounter_id": "{{cache:encounters.create.response.data.encounter.encounter_id}}"
      },
      "payload": {
        "grantee_persona_id": "{{cache:personas.create.response.data.persona.persona_id}}",
        "method": "{{static:chart.read}}"
      },
      "expectedStatus": 200
    }
  ]
}
