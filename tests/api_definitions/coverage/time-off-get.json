{
  "sdk": "sdk-coverage",
  "sourceFile": "packages/sdk-coverage/src/server/routes.ts",
  "requiresAuth": true,
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "epicId": "75f3f5a3-faa8-4b82-badf-4feda46c00e7",
  "featureId": "5f6dc00d-cb8f-4a54-9a19-9b1e0f0b2f5a",
  "taskId": "422472b0-117e-4cb0-9f90-0c6962439155",
  "testability": "auto",
  "name": "GET /api/coverage/time-off - List time off",
  "endpoint": "/api/coverage/time-off",
  "method": "GET",
  "expectedStatus": 200,
  "priority": 12,
  "description": "Time-off records for the tenant, optionally narrowed to one persona via the persona_id query parameter. Returns 200 with data.time_off as an array; an empty tenant answers 200 with [] rather than 404, because 'this persona has booked no leave' is an answer, not a missing resource. The tenant is taken from the verified claim: a tenant_id in the query that disagrees with the credential is 403, not silently preferred, and a caller with neither is 400. Unlike the POST, this route performs no date parsing, so it has no 422 path - the only refusals are the auth and tenant-resolution ones below.",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/coverage/time-off"
  ],
  "fieldEnums": {
    "kind": [
      "PTO",
      "MEETING",
      "OUTAGE",
      "HOLIDAY"
    ]
  },
  "testCases": [
    {
      "name": "List every time-off record in the tenant",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "queryParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "expectedStatus": 200,
      "expectedResponse": {
        "data": {
          "time_off": "array"
        }
      }
    }
  ],
  "errorCases": [
    {
      "name": "Unauthenticated",
      "queryParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}"
      },
      "expectedStatus": 401,
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "No Authorization: Bearer header - requireAuth preHandler, and the gateway default-deny authGate ahead of it"
    },
    {
      "status": 403,
      "code": "Forbidden",
      "message": "tenant_id does not match the authenticated tenant",
      "when": "The query names a tenant_id different from the credential's tenant claim - tenantOf() refuses rather than preferring either side"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "tenant_id is required",
      "when": "The credential carries no tenant_id claim and none is supplied in the query"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "listTimeOff throws a non-domain error - a database failure; domain errors are 422 but this read path raises none"
    }
  ]
}
