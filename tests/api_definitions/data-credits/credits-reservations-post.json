{
  "sdk": "sdk-data-credits",
  "sourceFile": "packages/sdk-data-credits/src/server/routes.ts",
  "requiresAuth": true,
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "epicId": "fefd6ec9-2fd1-4d19-8fa0-fbfb090590bb",
  "featureId": "51042c0e-c450-4b84-9a22-9c09ec7b9f44",
  "taskId": "5604f877-e1ba-40bb-b324-44f43ebcb1ac",
  "testability": "semi-auto",
  "priority": 15,
  "name": "POST /api/credits/reservations - Hold credits for a lookup you will run yourself",
  "endpoint": "/api/credits/reservations",
  "method": "POST",
  "expectedStatus": 201,
  "description": "The MANUAL lane. Holds the quoted credits for a capability that the caller will execute and settle itself, and returns the balance as it stands after the hold. It is deliberately distinct from POST /api/capability-requests + /execute, which is the BROKERED lane where the broker picks the provider, walks the fallback chain and settles for you; here the caller performs the lookup. Both lanes hold credits the same way and settle under the same rules - what differs is who does the looking. QA edge cases: the same governance applies, so a REQUEST_ONLY role gets a PENDING_APPROVAL reservation it cannot settle until it is approved; a hold that cannot be covered is 402 and writes nothing; the returned balance already reflects the hold, so available has gone down while balance has not moved.",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/personas"
  ],
  "setupScript": "data_credits_account.sql",
  "captureResponse": {
    "credits-reservations.create.reservation_id": "data.reservation_id",
    "credits-reservations.create.request_id": "data.request_id"
  },
  "testCases": [
    {
      "name": "Hold credits for a self-run lookup",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "capability_key": "{{var:data_credits_capability_key}}",
        "subject_fingerprint": "fp-{{dynamic:slug}}",
        "role_ref": "{{static:analyst}}",
        "requested_by_persona_id": "{{cache:personas.create.response.data.persona.persona_id}}",
        "metadata": {
          "lane": "manual"
        }
      },
      "expectedStatus": 201,
      "expectedResponse": {
        "data": {
          "reservation_id": "string",
          "estimated_credits": "number",
          "balance": {
            "available": "number"
          }
        }
      }
    }
  ],
  "errorCases": [
    {
      "name": "A hold with no capability_key is refused",
      "status": 400,
      "code": "VALIDATION_ERROR",
      "message": "capability_key is required",
      "when": "the payload omits capability_key or subject_fingerprint",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "subject_fingerprint": "fp-{{dynamic:slug}}"
      },
      "expectedStatus": 400,
      "expectedError": {
        "code": "VALIDATION_ERROR"
      }
    },
    {
      "status": 402,
      "code": "INSUFFICIENT_CREDITS",
      "message": "this request needs N credits and M are available",
      "when": "balance minus existing holds cannot cover the quote"
    },
    {
      "name": "Unauthenticated is refused by the default-deny gate",
      "status": 401,
      "code": "Unauthorized",
      "message": "missing or invalid tenant token",
      "when": "no valid tenant Bearer token on the request",
      "expectedStatus": 401,
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "capability_key": "{{var:data_credits_capability_key}}",
        "subject_fingerprint": "fp-x"
      }
    }
  ]
}
