{
  "name": "POST /api/admin/commands/dispatch-now — operator-triggered command dispatch pass",
  "endpoint": "/api/admin/commands/dispatch-now",
  "sdk": "sdk-command",
  "sourceFile": "services/api-gateway/src/app.ts",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "priority": 45,
  "dependsOn": [],
  "testability": "auto",
  "captureResponse": {
    "admin.commands.dispatch.response.data.dispatched": "data.dispatched"
  },
  "testCases": [
    {
      "name": "Operator runs one dispatch pass",
      "headers": {
        "x-admin-ops-token": "{{var:ADMIN_OPS_TOKEN}}"
      },
      "payload": {},
      "expectedStatus": 200
    },
    {
      "name": "Rejects a request with no admin ops token",
      "testType": "negative",
      "coversErrorCase": "401|admin token required",
      "headers": {},
      "payload": {},
      "expectedStatus": 401
    }
  ],
  "description": "Runs ONE pass of the command dispatcher immediately, flipping every command in 'approved' to 'dispatched' and publishing each onto its per-asset delivery channel. Header-auth gated with ADMIN_OPS_TOKEN via checkAdminToken, mirroring /admin/storm/ingest-now and /api/admin/asset/rollup/backfill: it lets an operator force a background worker to run now rather than waiting for its cadence (COMMAND_DISPATCH_INTERVAL_MS, default 2s). Returns data.dispatched — the count of commands moved. Idempotent by construction: dispatchApprovedCommands only matches rows in 'approved', so an immediate second call dispatches nothing and returns 0. This endpoint also makes the 'dispatched' state reachable by request at all; before it existed the state was produced only by the periodic tick, so POST /api/commands/:command_id/ack (which requires status='dispatched') could only race that tick and failed intermittently. Edge cases: a missing or wrong x-admin-ops-token is a 401 and dispatches nothing; with no approved commands the call still succeeds with dispatched=0; the batch is capped by COMMAND_DISPATCH_BATCH (default 50), so a backlog larger than that needs repeated calls.",
  "errorCases": [
    {
      "status": 401,
      "code": "admin token required",
      "message": "admin token required",
      "when": "The x-admin-ops-token header is absent, empty, or does not match ADMIN_OPS_TOKEN",
      "reproducible": true
    },
    {
      "status": 500,
      "code": "<underlying error message>",
      "message": "Dispatch pass failed",
      "when": "dispatchApprovedCommands throws — a database error while selecting or updating command.command",
      "reproducible": false
    }
  ]
}
