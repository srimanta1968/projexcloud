{
  "name": "POST /admin/federation/iceberg-backend — api-gateway",
  "endpoint": "/admin/federation/iceberg-backend",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "api-gateway",
  "sourceFile": "services/api-gateway/src/app.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Gated by the ADMIN_OPS_TOKEN (x-admin-ops-token header). driver=none hot-reloads to a no-op backend; nessie/glue additionally require ICEBERG_BACKEND_BASE_URL.",
  "testCases": [
    {
      "name": "Hot-reload the iceberg backend driver",
      "headers": {
        "x-admin-ops-token": "{{static:ADMIN_OPS_TOKEN}}"
      },
      "payload": {
        "driver": "{{static:none}}"
      },
      "expectedStatus": 200
    }
  ],
  "fieldOptions": {
    "driver": ["nessie", "glue", "none"]
  }
}
