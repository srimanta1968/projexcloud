{
  "name": "POST /admin/webhooks/dlq/:delivery_id/replay — api-gateway",
  "endpoint": "/admin/webhooks/dlq/:delivery_id/replay",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "api-gateway",
  "sourceFile": "services/api-gateway/src/app.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Gated by the ADMIN_OPS_TOKEN (x-admin-ops-token header). Reads no body; delivery_id is a path FK to a dead-lettered webhook delivery.",
  "testCases": [
    {
      "name": "Replay a dead-lettered webhook delivery",
      "headers": {
        "x-admin-ops-token": "{{static:ADMIN_OPS_TOKEN}}"
      },
      "pathParams": {
        "delivery_id": "{{static:REPLACE_WITH_DELIVERY_ID}}"
      },
      "expectedStatus": 200
    }
  ]
}
