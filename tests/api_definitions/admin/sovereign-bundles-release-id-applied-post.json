{
  "name": "POST /admin/sovereign/bundles/:release_id/applied — api-gateway",
  "endpoint": "/admin/sovereign/bundles/:release_id/applied",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "api-gateway",
  "sourceFile": "services/api-gateway/src/app.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Gated by the ADMIN_OPS_TOKEN (x-admin-ops-token header). Reads no body; release_id is a path FK to a previously-recorded sovereign bundle release.",
  "testCases": [
    {
      "name": "Mark a sovereign bundle release as applied",
      "headers": {
        "x-admin-ops-token": "{{static:ADMIN_OPS_TOKEN}}"
      },
      "pathParams": {
        "release_id": "{{static:REPLACE_WITH_RELEASE_ID}}"
      },
      "expectedStatus": 200
    }
  ]
}
