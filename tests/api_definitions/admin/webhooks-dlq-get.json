{
  "name": "GET /admin/webhooks/dlq — api-gateway",
  "endpoint": "/admin/webhooks/dlq",
  "method": "GET",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "api-gateway",
  "sourceFile": "services/api-gateway/src/app.ts",
  "_generated": true,
  "testability": "auto",
  "testCases": [
    {
      "name": "List dead-lettered webhook deliveries across tenants (operator view)",
      "expectedStatus": 200,
      "headers": {
        "x-admin-ops-token": "${ADMIN_OPS_TOKEN}"
      }
    }
  ]
}
