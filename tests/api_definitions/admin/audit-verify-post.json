{
  "name": "POST /admin/audit/verify — api-gateway",
  "endpoint": "/admin/audit/verify",
  "method": "POST",
  "requiresAuth": false,
  "expectedStatus": 200,
  "sdk": "api-gateway",
  "sourceFile": "services/api-gateway/src/app.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "Gated by the ADMIN_OPS_TOKEN (x-admin-ops-token header). Reads no body; tenant_id is a query param and must reference a tenant with an existing audit chain.",
  "testCases": [
    {
      "name": "Verify a tenant's audit hash-chain",
      "headers": {
        "x-admin-ops-token": "{{static:ADMIN_OPS_TOKEN}}"
      },
      "queryParams": {
        "tenant_id": "{{static:00000000-0000-0000-0000-000000000001}}"
      },
      "expectedStatus": 200
    }
  ]
}
