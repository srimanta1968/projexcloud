{
  "name": "GET /api/profile/secure-data/:person_id - Read Secure Data band",
  "endpoint": "/api/profile/secure-data/:person_id",
  "sdk": "sdk-profile",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "dc2e5cf3-1b7b-4dad-ae74-78b9f5ed6d14",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "027ceadb-5598-4071-8231-2cf5fa941bc2",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 60,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/profile/secure-data/set-field"
  ],
  "testCases": [
    {
      "name": "Read secure data",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "person_id": "{{cache:auth.register.response.data.userId}}"
      },
      "expectedStatus": 200
    }
  ],
  "description": "Reads the secure-data record for a person - the per-field encrypted envelopes plus the per-field state map (active / shredded) - returning 200 with { data: { secure_data } }. The row is created lazily by set-field, so a person who has never had a secure field written has no row at all. Edge cases: a person with no secure_data row is a 404; a person whose only fields have been shredded still returns 200, with the envelopes removed but the field_states entries retained as shredded (shredding never deletes the row); a non-UUID person_id hits the Postgres UUID cast unguarded and surfaces as a 500; no tenant scoping is applied; requires a valid JWT.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification or is expired (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "NotFound",
      "when": "No profile.secure_data row exists for the person_id (no field has ever been set)"
    },
    {
      "status": 500,
      "code": "Internal Server Error",
      "message": "invalid input syntax for type uuid",
      "when": "person_id is not a valid UUID - no format guard, so Postgres 22P02 escapes as an unhandled 500"
    }
  ]
}
