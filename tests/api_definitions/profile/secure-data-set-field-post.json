{
  "name": "POST /api/profile/secure-data/set-field - Set a Secure Data field envelope",
  "endpoint": "/api/profile/secure-data/set-field",
  "sdk": "sdk-profile",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "dc2e5cf3-1b7b-4dad-ae74-78b9f5ed6d14",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "027ceadb-5598-4071-8231-2cf5fa941bc2",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 40,
  "dependsOn": [
    "POST /api/auth/register"
  ],
  "captureResponse": {
    "profile.set-field.response.data.secure_data.person_id": "data.secure_data.person_id"
  },
  "testCases": [
    {
      "name": "Set PAN field envelope",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "person_id": "{{cache:auth.register.response.data.userId}}",
        "field_name": "pan",
        "envelope": "cGFuLXNlY3JldA=="
      },
      "expectedStatus": 200
    }
  ],
  "description": "Sets or replaces one named encrypted field on a person's secure-data record, creating the row on first use and merging the field into the existing field_envelopes / field_states JSONB on subsequent calls. Returns 200 with the full updated record. Idempotent: re-posting the same field overwrites in place and the field state is (re-)set to active - which means a previously shredded field can be resurrected by writing it again. Edge cases: person_id, field_name and envelope are all mandatory and an empty string counts as missing; envelope is stored verbatim with no format or size validation; a person_id that does not exist trips a foreign-key violation that escapes as an unhandled 500; requires a valid JWT.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification or is expired (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "missing required fields",
      "when": "person_id, field_name or envelope is absent or an empty string"
    },
    {
      "status": 500,
      "code": "Internal Server Error",
      "message": "insert or update on table \"secure_data\" violates foreign key constraint",
      "when": "person_id does not reference an existing person (or is not a valid UUID) - the upsert has no pre-check, so the Postgres error escapes unhandled"
    }
  ]
}
