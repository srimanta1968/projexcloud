{
  "name": "POST /api/impersonation/request - Support requests impersonation",
  "endpoint": "/api/impersonation/request",
  "sdk": "sdk-identity",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "fbcd8222-760d-445d-97d7-762c8709ccdd",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "f493450e-8ed8-425c-b905-f8a76a70bdcc",
  "epicId": "00996d23-f667-4a82-9285-7728ba357e7b",
  "priority": 30,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "testCases": [
    {
      "name": "Support engineer requests 30min impersonation",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "payload": {
        "support_user_id": "{{cache:auth.signup-tenant.response.data.userId}}",
        "target_tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "ticket_ref": "SUP-998",
        "duration_minutes": 30
      },
      "expectedStatus": 201
    }
  ],
  "expectedResponse": {
    "data": {
      "grant": {
        "grant_id": "string",
        "support_user_id": "string",
        "target_tenant_id": "string",
        "ticket_ref": "string",
        "manager_approval_id": "string",
        "customer_consent_ref": "string",
        "expires_at": "string",
        "certificate_audit_id": "string",
        "status": "string"
      }
    }
  },
  "statusTransitions": {
    "entity": "impersonation_grant",
    "field": "status",
    "flow": [
      "pending_approval",
      "active",
      "ended"
    ],
    "transitions": [
      {
        "from": null,
        "to": "pending_approval",
        "via": "POST /api/impersonation/request"
      },
      {
        "from": "pending_approval",
        "to": "active",
        "via": "POST /api/impersonation/:grant_id/approve"
      },
      {
        "from": "active",
        "to": "ended",
        "via": "POST /api/impersonation/:grant_id/end"
      }
    ]
  },
  "captureResponse": {
    "impersonation.request.response.data.grant.grant_id": "data.grant.grant_id",
    "impersonation.request.response.data.grant.status": "data.grant.status"
  },
  "description": "Opens a support impersonation grant against a target tenant, recording support_user_id, target_tenant_id and the ticket_ref, and returns 201 with the grant. Requires a valid tenant JWT (requireAuth). duration_minutes is optional and clamped server-side to the 5..240 range (default 30), so 1 becomes 5 and 10000 becomes 240 rather than erroring. The new grant is always created in status \"pending_approval\" because manager_approval_id and customer_consent_ref are still null - it does not grant access yet. Edge cases: support_user_id, target_tenant_id and ticket_ref are presence-checked only (no UUID or existence check), so a non-existent or malformed target_tenant_id passes validation and fails on the INSERT as a 500; the caller JWT identity is never compared to support_user_id, so a caller can open a grant naming someone else; there is no duplicate suppression - repeating the same ticket_ref creates another grant.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "support_user_id, target_tenant_id, ticket_ref are required",
      "when": "any of support_user_id, target_tenant_id or ticket_ref is absent or empty"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "the INSERT into identity.impersonation_grant throws - a non-UUID support_user_id or target_tenant_id failing the uuid cast, an unsatisfied foreign key, or any DB error"
    }
  ]
}
