{
  "name": "POST /api/role-assignments/:assignment_id/revoke - Revoke role assignment",
  "endpoint": "/api/role-assignments/:assignment_id/revoke",
  "sdk": "sdk-rebac",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "c0fb5ae9-73b6-498e-9f9e-426edd3d9755",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "b04da8c5-aa75-47ec-9bd6-63df4200a351",
  "epicId": "93a549d6-9a93-4d82-86bb-bac5167fa960",
  "priority": 85,
  "testability": "auto",
  "sourceFile": "packages/sdk-persona/src/server/routes.ts",
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/signup-tenant",
    "POST /api/app-identities",
    "POST /api/memberships",
    "POST /api/personas",
    "POST /api/role-assignments"
  ],
  "testCases": [
    {
      "name": "Revoke role assignment",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "assignment_id": "{{cache:role-assignments.create.response.data.assignment.assignment_id}}"
      },
      "payload": {},
      "expectedStatus": 200
    }
  ],
  "captureResponse": {
    "role-assignments.revoke.response.data.assignment.assignment_id": "data.assignment.assignment_id",
    "role-assignments.revoke.response.data.assignment.revoked_at": "data.assignment.revoked_at"
  },
  "description": "Revokes an active role assignment by stamping persona.role_assignment.revoked_at = now() and emitting identity.role.revoked.v1; returns 200 with the revoked assignment record. The UPDATE is guarded by `revoked_at IS NULL`, so it only affects assignments that are still active. Edge cases: revoking is NOT idempotent from the caller's view — a second revoke of the same assignment matches zero rows and returns 404, as does an assignment_id that never existed; a non-UUID assignment_id is rejected by Postgres and surfaces as an uncaught 500; there is no tenant/persona ownership check in this handler beyond the JWT gate, so scoping relies on the caller holding the assignment_id. Requires a valid tenant JWT.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "No Authorization: Bearer header — the gateway default-deny authGate runs requireAuth on every non-allowlisted path"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past)"
    },
    {
      "status": 404,
      "code": "NotFound",
      "message": "NotFound",
      "when": "No persona.role_assignment row with that assignment_id and revoked_at IS NULL — unknown id, or the assignment was already revoked"
    },
    {
      "status": 500,
      "code": "Internal Server Error",
      "message": "Fastify default error payload from the uncaught service throw",
      "when": "revokeRoleAssignment throws — assignment_id is not a valid UUID, or the UPDATE / audit emit fails"
    }
  ]
}
