{
  "name": "POST /api/break-glass/:grant_id/use - exercise grant, emit certificate-of-action",
  "endpoint": "/api/break-glass/:grant_id/use",
  "sdk": "sdk-approval",
  "sourceFile": "packages/sdk-approval/src/server/routes.ts",
  "method": "POST",
  "requiresAuth": true,
  "taskId": "e674989a-98de-4f93-99d4-186d56d24d9e",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "featureId": "d00b18c5-a75a-4a02-8e0e-b6f941ac60c1",
  "epicId": "0d61fa89-b842-43af-baf9-42454044726f",
  "expectedStatus": 200,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/approvals/routes",
    "POST /api/break-glass",
    "POST /api/break-glass/:grant_id/decide"
  ],
  "testCases": [
    {
      "name": "Exercise the active grant within scope, emitting a certificate",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "pathParams": {
        "grant_id": "{{cache:break-glass.request.response.data.grant.grant_id}}"
      },
      "payload": {
        "action": "read",
        "target_id": "{{var:target_id}}"
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "certificate": {
        "grant_id": "string",
        "cert_hash": "string"
      }
    }
  },
  "captureResponse": {
    "break-glass.use.response.data.certificate.grant_id": "data.certificate.grant_id",
    "break-glass.use.response.data.certificate.cert_hash": "data.certificate.cert_hash"
  },
  "description": "Exercises an approved break-glass grant to perform one emergency action against an optional target, returning an audit certificate that records what was done, by which persona and when. Edge cases: action is mandatory at the route; every failure from the service is surfaced as 403 (not 400) because a refusal here is an authorization denial — an unknown grant_id, a grant that is still pending or already consumed (\"is <status>, not active\"), a grant whose TTL has elapsed (\"has expired\"), and an action that falls outside the grant's recorded scope all return 403 with the specific reason; the acting persona is taken from the JWT rather than the body so a caller cannot attribute the use to someone else; target_id is optional, so scope checks that depend on a target must encode that in the scope object.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "No Authorization: Bearer <jwt> header on a requireAuth route"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past)"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "action is required",
      "when": "The body omits action or supplies an empty value"
    },
    {
      "status": 403,
      "code": "BreakGlass",
      "message": "grant <grant_id> not found",
      "when": "No break-glass grant exists for the path grant_id"
    },
    {
      "status": 403,
      "code": "BreakGlass",
      "message": "grant <grant_id> has expired",
      "when": "The grant was approved but its TTL window has elapsed"
    },
    {
      "status": 403,
      "code": "BreakGlass",
      "message": "grant <grant_id> is <status>, not active",
      "when": "The grant is still pending approval, was rejected, or has already been consumed"
    },
    {
      "status": 403,
      "code": "BreakGlass",
      "message": "action '<action>' is outside the grant scope",
      "when": "The requested action is not permitted by the scope object recorded on the grant"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "useBreakGlass throws a non-BreakGlassError (certificate write failure, database unreachable)"
    }
  ]
}
