{
  "name": "POST /api/commands — issue a command to a robot asset/component",
  "endpoint": "/api/commands",
  "sdk": "sdk-command",
  "sourceFile": "services/api-gateway/src/app.ts",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "2714a4d2-08d5-41cd-99ac-5677bd53c6d4",
  "sprintId": "efcf9cbd-5e84-469e-946e-efcabf8c88e1",
  "priority": 40,
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/assets"
  ],
  "testability": "auto",
  "fieldEnums": {
    "risk_class": [
      "low",
      "medium",
      "high",
      "critical"
    ]
  },
  "statusTransitions": {
    "entity": "command.command",
    "field": "status",
    "flow": [
      "pending",
      "approved",
      "rejected",
      "dispatched",
      "acked",
      "failed",
      "expired",
      "cancelled"
    ],
    "transitions": [
      {
        "from": null,
        "to": "approved",
        "via": "POST /api/commands"
      },
      {
        "from": null,
        "to": "pending",
        "via": "POST /api/commands"
      },
      {
        "from": "pending",
        "to": "approved",
        "via": "POST /api/commands/:command_id/decision"
      },
      {
        "from": "pending",
        "to": "rejected",
        "via": "POST /api/commands/:command_id/decision"
      },
      {
        "from": "approved",
        "to": "dispatched",
        "via": "INTERNAL sdk-command background dispatcher"
      },
      {
        "from": "dispatched",
        "to": "acked",
        "via": "POST /api/commands/:command_id/ack"
      },
      {
        "from": "dispatched",
        "to": "failed",
        "via": "POST /api/commands/:command_id/ack"
      }
    ]
  },
  "captureResponse": {
    "commands.create.response.data.command_id": "data.command_id",
    "commands.create.response.data.status": "data.status"
  },
  "testCases": [
    {
      "name": "Issue a low-risk move command",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "target_asset_id": "{{cache:assets.create.response.data.asset_id}}",
        "target_component_id": "{{var:component_id}}",
        "type": "move",
        "params": {
          "x": 1,
          "y": 2
        },
        "risk_class": "high"
      },
      "expectedStatus": 201
    }
  ],
  "description": "Issues a command to a robot asset or one of its components. The request is authorized through ReBAC plus policy inside sdk-command; commands whose risk_class is high enough land in a pending state awaiting approval via POST /api/commands/:command_id/decision instead of dispatching immediately. Returns 201 with the command record. tenant_id falls back to the JWT tenant_id claim; the issuer is always the JWT sub. Edge cases: a token with no tenant_id or no sub is a 400; target_asset_id and type are both mandatory; a ReBAC/policy denial is a 403 carrying the authorization error message; an unknown target_asset_id or target_component_id is not pre-checked and surfaces as a generic 500; params and risk_class are optional.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification or is expired (requireAuth / gateway default-deny authGate)"
    },
    {
      "status": 400,
      "code": "tenant_id required",
      "message": "tenant_id required",
      "when": "Neither body.tenant_id nor the JWT tenant_id claim is present"
    },
    {
      "status": 400,
      "code": "issuer identity required",
      "message": "issuer identity required",
      "when": "The JWT carries no sub claim to record as issued_by"
    },
    {
      "status": 400,
      "code": "target_asset_id and type are required",
      "message": "target_asset_id and type are required",
      "when": "body.target_asset_id or body.type is absent or empty"
    },
    {
      "status": 403,
      "code": "<CommandAuthorizationError message>",
      "message": "Command authorization denied",
      "when": "issueCommand throws CommandAuthorizationError - the issuer lacks the ReBAC relation or the policy engine denied this command type/risk class for the asset"
    },
    {
      "status": 500,
      "code": "<underlying error message>",
      "message": "Command issuance failed",
      "when": "Any other issueCommand failure - unknown/non-UUID target_asset_id, unknown target_component_id, or database error"
    }
  ]
}
