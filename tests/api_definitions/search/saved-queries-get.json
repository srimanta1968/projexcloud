{
  "name": "GET /api/search/saved-queries - List a persona's saved queries",
  "endpoint": "/api/search/saved-queries",
  "sdk": "sdk-search",
  "sourceFile": "packages/sdk-search/src/server/routes.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "172643e9-507b-48df-b52b-1cad6f74db4f",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "02447110-6a41-4546-8d10-2107aa023f3a",
  "epicId": "d83f7b6e-821c-4fe1-96e6-910eca8fbbfc",
  "priority": 50,
  "testability": "auto",
  "dependsOn": [
    "POST /api/auth/signup-tenant",
    "POST /api/search/saved-queries"
  ],
  "testCases": [
    {
      "name": "List my saved queries",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "queryParams": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "persona_id": "{{cache:auth.signup-tenant.response.data.userId}}"
      },
      "expectedStatus": 200
    }
  ],
  "expectedResponse": {
    "data": {
      "queries": "array"
    }
  },
  "description": "Lists the saved queries belonging to one persona within the caller's tenant, scoped by the JWT tenant_id plus the required persona_id query param. Edge cases: persona_id is mandatory and a missing or blank value is a 400 — it is not treated as \"list all\"; a persona that exists but owns nothing returns 200 with an empty queries array rather than 404; a persona_id from another tenant yields an empty list because the tenant filter comes from the JWT, so this endpoint cannot be used to enumerate other tenants' saved queries; there is no pagination, so the full set for the persona is returned in one response.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "No Authorization: Bearer <jwt> header on a requireAuth route"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails verifyJwt (bad signature, malformed, or exp in the past)"
    },
    {
      "status": 403,
      "code": "Forbidden",
      "message": "JWT missing tenant_id claim",
      "when": "Token verifies but carries no tenant_id claim; authTenant() rejects before any query runs"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "persona_id required",
      "when": "persona_id query param is missing or empty"
    },
    {
      "status": 404,
      "code": "IndexNotFound",
      "message": "Referenced index could not be resolved",
      "when": "listSavedQueries throws IndexNotFoundError"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "The datastore read fails or throws a non-IndexNotFoundError"
    }
  ]
}
