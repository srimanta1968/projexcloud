{
  "name": "POST /api/search — sdk-search",
  "endpoint": "/api/search",
  "method": "POST",
  "requiresAuth": true,
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/search/index"
  ],
  "expectedStatus": 200,
  "sdk": "sdk-search",
  "sourceFile": "packages/sdk-search/src/server/routes.ts",
  "_generated": true,
  "testability": "manual",
  "skipReason": "ABAC free-text/DSL query against OpenSearch; requires a real OpenSearch cluster (or in-process synthetic client) and a registered index for the entity_kind (POST /api/search/index). tenant_id and effective_scopes are server-injected from the JWT, never the body. Required body field (entity_kind) filled.",
  "testCases": [
    {
      "name": "Query encounter entities",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "payload": {
        "entity_kind": "{{static:encounter}}",
        "q": "{{dynamic:word}}",
        "size": 10
      },
      "expectedStatus": 200
    }
  ]
}
