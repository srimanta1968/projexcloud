{
  "endpoint": "/api/agent-runtime/runs/:run_id/rollback",
  "method": "POST",
  "taskId": "d5cab8c0-26bf-462e-9a49-d11aed1552e8",
  "description": "Roll back an agent run by replaying agents.action_journal in reverse from latest step down to ?to_seq=N. Invokes per-action_type compensation handlers. Returns RollbackSummary. AC-8.",
  "requiresAuth": true,
  "priority": 210,
  "dependsOn": ["POST /api/auth/register"],
  "testability": "manual",
  "skipReason": "Rollback requires a populated agent_run + action_journal fixture; exercised by AC-8 chaos drill.",
  "testCases": [
    {
      "name": "Manual: rollback latest 3 steps of a run",
      "headers": {
        "Authorization": "Bearer {{cache:auth.register.response.data.token}}"
      },
      "pathParams": {
        "run_id": "{{static:fixture-run-id}}"
      },
      "payload": {
        "reason": "{{static:Operator-initiated rollback for testing}}"
      },
      "expectedStatus": 200,
      "expectedResponse": {
        "success": true
      }
    }
  ]
}
