{
  "name": "POST /api/payments/methods - Attach PCI-tokenized payment method",
  "endpoint": "/api/payments/methods",
  "sdk": "sdk-payment",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "2d3f74a8-f5f9-4765-a598-3c2adc5bf755",
  "sprintId": "8ef1d09e-dd8e-4daf-8d03-54dcd9f812f8",
  "featureId": "33deeb13-adc2-4198-b1e1-1315ca492994",
  "epicId": "d83f7b6e-821c-4fe1-96e6-910eca8fbbfc",
  "priority": 20,
  "testability": "auto",
  "_fixNote": "provider_token MUST be unique per run. attachPaymentMethod upserts ON CONFLICT (provider, provider_token); a constant token ('pm_card_visa') makes every run after the first hit DO UPDATE and RETURN the FIRST run's row (owned by that first tenant). POST /api/payments/charge then 403s (TenantOwnership) because the returned method belongs to a different tenant than the current JWT. The {{dynamic:slug}} suffix forces a fresh insert owned by the current signup tenant. The synthetic stripe adapter accepts any non-'decline_' token.",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "fieldEnums": {
    "provider": [
      "stripe",
      "razorpay",
      "plaid",
      "ach"
    ],
    "kind": [
      "card",
      "bank-account",
      "upi",
      "wallet"
    ]
  },
  "testCases": [
    {
      "name": "Attach Stripe card via tokenized ref",
      "headers": {
        "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}"
      },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "persona_id": "{{cache:auth.signup-tenant.response.data.userId}}",
        "provider": "stripe",
        "provider_token": "pm_card_visa_{{dynamic:slug}}",
        "kind": "card",
        "last4": "4242",
        "brand": "visa",
        "exp_month": 12,
        "exp_year": 2030,
        "secure_data_field_ref": "{{var:secure_data_field_ref}}"
      },
      "expectedStatus": 201
    }
  ],
  "expectedResponse": {
    "data": {
      "method": {
        "method_id": "string",
        "provider": "string",
        "last4": "string"
      }
    }
  },
  "captureResponse": {
    "payments.methods.create.response.data.method.method_id": "data.method.method_id"
  },
  "description": "Attaches a tokenized payment method to a persona and returns 201 with the stored method. Requires a valid tenant JWT (requireAuth); tenant_id is forced from the JWT tenant_id claim and any body tenant_id is overwritten, so a method cannot be attached to another tenant. Edge cases: a JWT without a tenant_id claim (for example a login with no tenant selected) is a 403 rather than a 500; provider must be one of the allowed providers and kind one of the allowed kinds, so an unknown value is a 400 listing the permitted set; provider_token is required and is additionally screened for raw-card-number shape - sending a PAN instead of a provider token is refused per FR-PAY-2 as a 400 and nothing is stored; persona_id is presence-checked only, so a non-existent persona surfaces from the service rather than the validator; validation accumulates, so one 400 may list several messages in details[]; the endpoint is not idempotent - re-posting the same provider_token attaches another method.",
  "errorCases": [
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Missing bearer token",
      "when": "Authorization header absent or not in Bearer form (requireAuth)"
    },
    {
      "status": 401,
      "code": "Unauthorized",
      "message": "Invalid or expired token",
      "when": "Bearer token fails JWT verification (requireAuth)"
    },
    {
      "status": 403,
      "code": "Forbidden",
      "message": "JWT missing tenant_id claim",
      "when": "req.auth.tenant_id is absent after auth - the token carries no tenant scope"
    },
    {
      "status": 400,
      "code": "ValidationError",
      "message": "tenant_id is required / persona_id is required / provider must be one of <list> / provider_token is required / kind must be one of <list> / provider_token looks like a raw card number; refusing per FR-PAY-2 (use the provider token only)",
      "when": "validateAttachMethod fails; details[] carries every failed rule"
    },
    {
      "status": 500,
      "code": "InternalError",
      "message": "InternalError",
      "when": "attachPaymentMethod throws an unmapped error - provider API failure, an unsatisfied persona foreign key, or any DB error"
    }
  ]
}
