{
  "name": "POST /api/incidents - Open an incident",
  "endpoint": "/api/incidents",
  "sdk": "sdk-incident",
  "sourceFile": "packages/sdk-incident/src/server/routes.ts",
  "method": "POST",
  "requiresAuth": true,
  "expectedStatus": 201,
  "taskId": "da328f90-4933-4b2f-a642-e410a5b39ec9",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "priority": 50,
  "testability": "auto",
  "description": "Open an operational incident in status 'open'. tenant_id, incident_type and title are required; severity (low|medium|high|critical, default medium), description, affected_records, owner/reported_by personas, source, subject_ref, sla_due_at (SLA deadline for the breach scan) and metadata are optional. Emits incident.opened.v1. Advance via POST /api/incidents/:incident_id/transition.",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "fieldEnums": {
    "severity": ["low", "medium", "high", "critical"]
  },
  "testCases": [
    {
      "name": "Open a high-severity data-quality incident",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "payload": {
        "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}",
        "incident_type": "data-quality",
        "title": "Bad import batch flagged",
        "severity": "high",
        "reported_by_persona_id": "{{cache:auth.signup-tenant.response.data.userId}}",
        "affected_records": ["batch-42"]
      },
      "expectedStatus": 201,
      "expectedResponse": { "data": { "incident": { "incident_id": "string", "status": "string" } } }
    }
  ],
  "captureResponse": {
    "incident.incidents.create.response.data.incident.incident_id": "data.incident.incident_id"
  },
  "errorCases": [
    { "status": 400, "code": "ValidationError", "message": "tenant_id, incident_type and title are required", "when": "tenant_id, incident_type or title missing from body" }
  ]
}
