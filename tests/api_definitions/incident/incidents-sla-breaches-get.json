{
  "name": "GET /api/incidents/sla-breaches - SLA-breach scan",
  "endpoint": "/api/incidents/sla-breaches",
  "sdk": "sdk-incident",
  "sourceFile": "packages/sdk-incident/src/server/routes.ts",
  "method": "GET",
  "requiresAuth": true,
  "expectedStatus": 200,
  "taskId": "da328f90-4933-4b2f-a642-e410a5b39ec9",
  "sprintId": "d4876947-8d18-4538-8e9f-51ba3e0735a9",
  "priority": 51,
  "testability": "auto",
  "description": "SLA-breach scan: returns tenant incidents whose sla_due_at is past AND whose status is still active (not resolved/closed/cancelled), ordered by deadline ascending. Powered by the incident_sla_idx partial index. Tenant-scoped via the required tenant_id query param; optional limit (default 100). Returns an empty list when nothing is overdue.",
  "dependsOn": [
    "POST /api/auth/signup-tenant"
  ],
  "testCases": [
    {
      "name": "Scan SLA breaches for the signup tenant",
      "headers": { "Authorization": "Bearer {{cache:auth.signup-tenant.response.data.token}}" },
      "queryParams": { "tenant_id": "{{cache:auth.signup-tenant.response.data.tenant_id}}" },
      "expectedStatus": 200,
      "expectedResponse": { "data": { "incidents": [] } }
    }
  ],
  "errorCases": [
    { "status": 400, "code": "ValidationError", "message": "tenant_id query param required", "when": "tenant_id query param missing" }
  ]
}
