# @projexlight scope: Verdaccio in dev, npm.projexcloud.com in prod.
# Set LOCAL_REGISTRY_TOKEN / NPM_TOKEN at publish time; default unset is fine for read.
@projexlight:registry=http://localhost:4873/
