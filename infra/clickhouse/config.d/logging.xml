<clickhouse>
    <!-- Bound internal server logs: default is trace level @ 1000M x10 (~20GB).
         Cap to information level, 100M x3 rotation per log (~600MB worst case). -->
    <logger>
        <level>information</level>
        <size>100M</size>
        <count>3</count>
    </logger>
</clickhouse>
