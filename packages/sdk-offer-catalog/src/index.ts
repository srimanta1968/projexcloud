/**
 * @projexlight/sdk-offer-catalog — versioned offer truth + feature-status matrix (P15·E1).
 *
 * v0 surface: migrationsDir only. The offer_feature matrix lands in migration
 * 002; the REST surface + publish/activate/resolve-current service land in the
 * later P15·E1 tasks.
 */
export { migrationsDir } from './db';
