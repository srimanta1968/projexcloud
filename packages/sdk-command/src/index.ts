/**
 * @projexlight/sdk-command — command & control plane for the physical-AI fleet
 * (P12 · E1). This drop lands the additive `command.command` schema; the issue/
 * authorize/dispatch/ack service surface follows in subsequent tasks.
 */
export { migrationsDir } from './db';
