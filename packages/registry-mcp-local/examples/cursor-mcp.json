{
  "_comment": "Drop this into <project>/.cursor/mcp.json. Cursor reads this on startup and exposes the tools to the inline AI.",
  "mcpServers": {
    "projex-registry": {
      "command": "npx",
      "args": ["-y", "@projexlight/registry-mcp-local"]
    }
  }
}
