{
  "_comment": "Drop this into ~/.claude/mcp.json (or merge into existing mcpServers map). After saving, restart Claude Code. The AI will then see 6 projex_registry_* tools alongside any other MCPs you have configured.",
  "mcpServers": {
    "projex-registry": {
      "command": "npx",
      "args": ["-y", "@projexlight/registry-mcp-local"]
    }
  }
}
