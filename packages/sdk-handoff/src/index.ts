/**
 * @projexlight/sdk-handoff — Sales→Delivery handoff with accept/reject (P15·E2).
 *
 * v0 surface: migrationsDir only. Handoff CRUD + lifecycle service, the
 * sdk-workflow saga, and the sdk-approval accept/reject gate land in the later
 * P15·E2 tasks.
 */
export { migrationsDir } from './db';
