/**
 * @projexlight/sdk-incident — exception/incident record & evidence (P15·E3).
 *
 * v0 surface: migrationsDir only. Incident CRUD + lifecycle + SLA scan, the
 * evidence timeline (→ sdk-audit) and REST surface land in the later P15·E3 tasks.
 */
export { migrationsDir } from './db';
