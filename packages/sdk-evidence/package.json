{
  "name": "@projexlight/sdk-evidence",
  "version": "1.0.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --config tests/vitest.config.ts"
  },
  "dependencies": {
    "@projexlight/contracts": "workspace:*",
    "@projexlight/db-runtime": "workspace:*",
    "@projexlight/sdk-audit": "workspace:*",
    "@projexlight/sdk-consent": "workspace:*",
    "@projexlight/sdk-device": "workspace:*",
    "@projexlight/sdk-engagement": "workspace:*",
    "@projexlight/sdk-media": "workspace:*",
    "@projexlight/sdk-meter": "workspace:*",
    "@projexlight/sdk-pool-router": "workspace:*",
    "@projexlight/sdk-vault": "workspace:*",
    "fastify": "^4.24.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0",
    "pg": "^8.11.3",
    "@types/pg": "^8.11.0",
    "@types/pdfkit": "^0.13.4"
  },
  "dependenciesNote": "pdfkit pulled in as a transitive runtime dep via the legal-export bundler. Kept in devDependencies so dev installs include it; production packaging adds it via the api-gateway image. Pure JS, no native bindings.",
  "optionalDependencies": {
    "pdfkit": "^0.15.0"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "dist",
    "README.md",
    "src/db/migrations"
  ]
}
