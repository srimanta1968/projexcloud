/**
 * Server-side surface for sdk-ai-gateway. The hosting service
 * (services/api-gateway for prototype; eventually
 * services/ai-gateway-service) imports `registerRoutes` and mounts it.
 */
export { registerRoutes } from './routes';
