{
  "server_port": 4000,
  "server_host": "host.docker.internal",
  "health_endpoint": "/health",
  "base_url": "http://host.docker.internal:4000",
  "auto_synced": false,
  "auto_detected": false,
  "sync_source": "manual_gateway_port_pin",
  "note": "api-gateway is pinned to :4000 via GATEWAY_PORT in root .env. Do NOT auto-sync/auto-detect this away from :4000. Other local processes ALSO answer GET /health with 200 and get wrongly chosen by port auto-detection: the Next.js portal on :3000 (404s every /api route) and the projexlight-test-mcp FastAPI on :8000 (returns {'detail':'Not Found'} for every route). The api tests MUST hit the gateway on :4000; keep auto_detected AND auto_synced false so the runner honors this pin instead of re-detecting."
}
