{
  "server_port": 4000,
  "server_host": "host.docker.internal",
  "health_endpoint": "/health",
  "base_url": "http://host.docker.internal:4000",
  "auto_synced": false,
  "auto_detected": false,
  "sync_source": "manual_gateway_port_pin",
  "note": "api-gateway is pinned to :4000 via GATEWAY_PORT in root .env. Do NOT auto-sync this to the Next.js portal on :3000 (it answers 404 on /health and 404s every /api route). The runner honors this hint first; 4000/health returns 200 from the dev-mcp container."
}
