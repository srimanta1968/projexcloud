# {{app_name}}

Insurance claims-intake workspace — scaffolded from **{{blueprint_title}}**.

## Configuration

| Choice | Value |
|---|---|
| Settlement payout path | `{{settlement_path}}` |
| SIU auto-flag keywords | `{{siu_flag_keywords}}` |
| Photo redaction on by default | `{{photo_redaction_default}}` |

## Layout

- `src/claims/intake.ts` — FNOL handler, SIU keyword flagger
- `src/claims/review.ts` — adjuster decision recording
- `db/migrations/001_claims_init.sql` — `claims.claim` + `claims.decision`

## Pack: finserv

Pack guardrails (when the FinServ pack is enabled on the tenant) auto-enforce
SOX retention + 4-eyes approval on any payment-touching action via sdk-approval.

## Next

```sh
projex install @projexlight/sdk-workflow
projex install @projexlight/sdk-evidence
projex install @projexlight/sdk-approval
projex install @projexlight/sdk-payment
projex install @projexlight/hdk-image-editor
projex install @projexlight/sdk-audit
```

Or re-apply with `--install-sdks` to do all six at once.
