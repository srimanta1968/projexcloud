# {{app_name}}

Field service dispatch workspace — scaffolded from **{{blueprint_title}}**.

## Configuration

| Choice | Value |
|---|---|
| Auto-assignment strategy | `{{assignment_strategy}}` |
| Weather hold threshold | `{{weather_hold_threshold}}` |
| Photo required on completion | `{{photo_capture_required}}` |

## What's here

- `src/jobs/dispatcher.ts` — job state machine + status fan-out
- `src/jobs/assignment.ts` — tech picker (strategy-driven)
- `db/migrations/001_field_init.sql` — `field.job` + `field.technician`

## Next

```sh
projex install @projexlight/sdk-dispatch
projex install @projexlight/sdk-assignment
projex install @projexlight/sdk-storm
projex install @projexlight/hdk-map
projex install @projexlight/hdk-camera
projex install @projexlight/sdk-notification
projex install @projexlight/sdk-evidence
```

Or re-apply with `--install-sdks` to do all seven at once.
