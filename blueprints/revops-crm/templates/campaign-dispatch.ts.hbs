/**
 * {{app_name}} — Campaign dispatch
 *
 * Default outbound channel: {{campaign_default_channel}}
 */

import type { ScoredLead } from '../scoring/model';

export interface Campaign {
  campaign_id: string;
  channel: 'email' | 'sms' | 'slack-mention' | 'multi-touch';
  template_id: string;
}

export async function dispatchCampaign(campaign: Campaign, leads: ScoredLead[]): Promise<void> {
  for (const lead of leads) {
    // Wire to sdk-notification / Slack connector / etc.
    // The {{campaign_default_channel}} answer from the blueprint apply
    // determines the default channel above.
    void lead;
    void campaign;
  }
}
